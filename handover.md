# HANDOVER — Skärgårdsspaning (sailing trip game)

**Purpose of this doc:** full context for continuing development in a new chat. Read this + skim `index.html` before making changes.

## What this is

Multiplayer deck game (PWA-style web app, Swedish UI) for Roope's sailing trip in the Turku archipelago, **departing Monday 2026-07-13**. Crew: Roope, Tiina, Pernilla (join by car at Hangö), Patrik & Martin (depart by boat from Storören). **Martin is captain.** Boat: s/y Sun-Day, Degerö 33 (LOA 9,81 m, beam 3,52 m, draft 1,5 m, mast 15 m, 5 200 kg, sloop).

Route (15 board tiles, editable by captain): STORÖREN → Barösund (Mon night, boat crew only) → Hangö (crew meetup "Mönstring") → Airisto → Själö → Brännskär → Korpoström → Aspö → Nötö → Björkö → Helsingholm → Högsåra → Örö → Dalsbruk → 🏁 KASNÄS (spa finale).

## Files (all in this folder)

| File | Role |
|---|---|
| `index.html` | The entire app (~2300 lines: CSS + HTML + one `<script>`) |
| `config.js` | Supabase URL + anon key (filled, working) |
| `schema.sql` | Full schema incl. `boats` — kept in sync with live migrations (last synced 2026-07-15) |
| `gemma_bridge.py` | Runs on Roope's home PC: polls chat, answers @gemma via local Ollama |
| `netlify/functions/mistral-fallback.mts` | **Added 2026-07-14** — Netlify Scheduled Function, cloud fallback for @gemma when the home PC is off (see below) |
| `boat.jpg` | Watercolor hero image (login screen) |
| `SETUP.md` | User-facing setup guide |
| `sounds/` (optional) | Not created yet — see Bird sounds below |

## Infrastructure (all live, all free tier)

- **Hosting:** Netlify site `sunday-spaning` → https://sunday-spaning.netlify.app (site id `ef94bbc0-ba0c-4443-b146-d582def9d81a`, team `soderhro`). **Updated 2026-07-14: now linked to GitHub** (`soderhro-source/Sk-rg-rds-spel`, branch `claude/game-improvements-review-rhb28k`) for auto-deploy on every push — the manual drag-the-folder workflow and the blocked npx-upload workaround are no longer needed, just `git push`. Base directory/build command/publish directory are all left empty (plain static site, no build step).
- **Backend:** Supabase project `wptovqfypslbneobnjim` (eu-west-2, "Söderholm's Org"), managed via the Supabase MCP connector. Tables: `boats` (name/emoji/captain/route — **added 2026-07-14 for multi-boat support**, see below), `sailors` (name+PIN hash+emoji+`boat_id` fk), `logs` (all points: kind ∈ spot/quiz/checkpoint/knot/surprise/minigame/bonus, unique indexes prevent duplicates; surprise unique per crew per day), `chat`, `port_notes` (harbor logbook incl. `photo_url`), `settings` (key/value jsonb — legacy singleton `captain`/`route` rows kept but no longer read by the app; route/captain now live per-boat on `boats`), `track_points` (GPS log: lat/lng/sog/ts). Storage bucket `photos` (public, anon upload). RLS: open for anon (friends-level security). Realtime enabled on logs, chat, settings, boats.
- **Auth:** name + 4-digit PIN, SHA-256 client-side. No email.
- **Offline-first:** localStorage write-queue for logs, auto-flush every 15 s + on `online`. Chat/photos/map need signal.

## Game design (current state)

- Candy Crush-style saga board, 15 nodes serpentine. **Everyone starts at Storören**; tile = `floor(points/30)` capped at 14; **MÅL at 420 p**. Passed harbors = green + ⭐, current = pulsing gold ring, fog-of-war ("— ? —") beyond next harbor. Crew ship ⛵ moves on sum of a **boat's own crew's** points (leg = 30 × that boat's crew size).
- **Multi-boat (added 2026-07-14):** friends on a second boat can join the same game. At "Mönstra på" you pick an existing boat or create a new one (name+emoji; creator auto-becomes its captain). Each boat has its own `route`, `captain`, saga-board position and leaderboard (`mySailors()`/`myBoat()` scope everything); the board tab shows a compact "Andra båtar" line with each other boat's points/position for a friendly-race feel. Species spotting/quiz/trivia/arcade stay global fun, not boat-scoped. `trkShowToday`/Törnrapport filter `track_points` to the viewer's own boat's sailor IDs so two boats' GPS tracks don't get mixed on the map. A boat with no captain shows "Bli kapten" to whoever's viewing it (self-serve, no pre-coordination needed). Seed boats: `Sun-Day` (captain Martin, original route) and `Vänbåten` (placeholder name/emoji — no captain yet, first joiner should rename via Supabase or just claim captain and keep the name).
- **Known gap:** the map (Seglandet tab) does not yet show live position markers for *other* boats — it only ever showed the viewing device's own GPS dot (this predates multi-boat). Real-time cross-boat position sharing on the map would need a new realtime subscription + colored markers; not built, flagged as a possible follow-up.
- **Crew roles (added 2026-07-14):** captain can assign crew to 6 roles (`ROLES` const: Rorsman/helmsman, Skeppskock/cook, Maskinchef/engineer, Ankringsmästare/anchor master, Utkik/lookout, Navigatör/navigator) via a panel in the 🎁 Dagens tab — reassignable anytime, stored in `boats.roles` (jsonb map roleKey→sailorId). Whoever holds a role sees "Din roll idag" with a rotating daily task (deterministic by date+role hash, `ROLE_TASKS` pool of 3 per role, same pattern as `SURPRISES`/`POPQUIZ`) and can log it once/day for 3 p (`kind='bonus'`, `ref='role:<roleKey>:<date>'`; DB-enforced via the `one_per_sailor` unique index, which now also covers `'bonus'`). Only the assigned holder can log their own role's task — no gating on other content. Role badges show next to names in the board leaderboard. Captain (the role, not one of the 6) is unchanged — still `boats.captain`/`claimCaptain()`, separate mechanism.
- Points sources: species spotting (~90 cards, 6 categories in one 🔭 Spana tab with chips, each card has 📷 facit Google-Images link), harbor challenges (+5; unlock by points OR **GPS within 1.5 km**; Storören "Avfärdskontroll" open from start), knots (6), navigationsskolan (6 lessons, kind='knot', ref='nav:i'), quiz (bookable 1×/day), daily pop-quiz overlay (+2, ship+seamark theory, 16 Qs), daily surprise (first-to-claim crew-wide), arcade: Lastrummet/Tetris ≤5 p/day, Skärgårdsminne/memory ≤4, Bojkrossen/match-3 ≤5, Ålen/snake ≤4, Uno-poäng (no game points — see below).
- **Sjömansvisor playlist (added 2026-07-14):** new 🎵 tab in Mer (`SONGS` const, 8 entries — Evert Taube classics + trad. sjömansvisor) linking to YouTube search results (not specific video URLs, so links never rot). Same `.scard` styling as Läten.
- **Uno-poäng (added 2026-07-14):** 5th tile in 🕹️ Spelrum, requested by Patrik — a standalone score reporter for the physical card game Uno, unrelated to the sailing game's point system (no `logs` entry, no daily cap). Add players, enter each round's penalty points (loser's remaining-card values; standard Uno scoring explained in the intro text), running totals sorted lowest-first (leader gets 👑). Persisted client-side only (`localStorage.ss_uno`) — not synced to Supabase, so it's per-device, resettable, and 100% independent of the trip scoring.
- **Skepparens loggbok / captain's log (added 2026-07-14):** new `captain_log` table (boat_id, sailor_id, departure/arrival place+time, miles, weather, notes), captain-only entry form inside the existing 📜 Törnrapport tab (`renderCaptainLogCard()`), scoped per boat like everything else post-multi-boat. Distinct from `port_notes` (the per-harbor star-rating logbook) — this is a proper leg-by-leg ship's log: who logged what, when they left/arrived, distance, weather (free text + quick chips), freeform notes. "Fyll i från spår" button pre-fills distance from the day's GPS track if available. Only the boat's captain sees the add/delete controls; entries are visible to the whole crew. **Bug fixed during this build:** the delete handler originally re-rendered the card before removing the old one, leaving two elements with the same `id` in the DOM simultaneously — invalid HTML that silently broke a scoped `querySelector` (caught by the jsdom smoke test, not by `node --check`, since it's syntactically valid JS). Fixed by removing the stale card first.
- Also fixed two latent boat-scoping bugs in `renderReport()` left over from the multi-boat change: the per-sailor breakdown and the "ship reached" tile were still reading the *global* `sailors` array instead of `mySailors()`, which would have mixed both boats' crews into one boat's report.
- **GPS auto-resume + wake lock (added 2026-07-14):** "⏺ Logga färd" preference now persists (`localStorage.ss_trk_rec`) and silently resumes (`trkResumeIfWanted()`, called from `enterApp()`) next time the app opens or a different sailor logs in on the same device — no more re-tapping every session. While recording, requests a Screen Wake Lock (re-acquired on `visibilitychange`) so the screen doesn't sleep mid-log. **Hard limit, by design of the web platform:** this is still foreground-only — a plain web app cannot log GPS while the browser tab is fully closed or the phone is locked/backgrounded for long, on iOS especially (no reliable background geolocation for web apps, PWA or not). Auto-resume removes the "forgot to tap record" friction; it does not make tracking work with the app closed. `logout()` now clears the active `watchPosition` (was a latent bug: pre-fix, staying "recording" through a logout would queue `track_points` rows with `sailor_id:null`).
- **Planeringsläge / planning mode (added 2026-07-14):** `canPlan()` = captain OR whoever holds the Navigatör role — both now see the board tab's route button (relabeled "🧭 Planeringsläge", was captain-only "Ändra ruttordning"). No new table; reuses `boats.route`. The old reorder-arrows UI is still there, now wrapped with: a short wind forecast (`windForecastAt()`, Open-Meteo `hourly` params, centered on current GPS fix or the next harbor if no fix) and a "next stop" card showing live distance/ETA (`distEtaTo()`, haversine from `lastFix` to the harbor's known coordinates, editable assumed speed input) plus a "🔀 Byt ut mot en annan hamn" swap picker pulling from `ALT_HARBORS`+`EXTRA_HARBORS`, sorted nearest-first when a GPS fix exists. Swapping only applies to non-goal slots (index 13 / NÅDENDAL stays locked, same as before). Changes sync live via the existing `saveRoute()`.
- **Alt/extra harbor coordinates researched (added 2026-07-14):** `ALT_HARBORS` (9) and `EXTRA_HARBORS` (15) now carry real `lat`/`lng` for 22 of 24 — verified via web search (official harbor sites, vierassatamat.fi, gasthamnar.fi, Navily, etc.), not guessed; one cross-check landed within 1.3 km of the already-verified `PLACES` entry for the same harbor (Nådendal), which gave good confidence in the sourcing. The 2 that couldn't be verified (**Kirjais**, **Villa Wolax** — address known, no dock-level coordinate published anywhere indexed) were deliberately left without `lat`/`lng` rather than estimated; the planning-mode swap picker shows "avstånd okänt" only for those two. `harborCoords()` now searches `PLACES`+`ALT_HARBORS`+`EXTRA_HARBORS` (was `PLACES`-only), so GPS-based harbor-arrival unlock (`nearHarbor()`) also now works if a captain/navigator swaps in one of these as a route stop.
- **Mobile overflow fix (added 2026-07-14, Roope reported elements going off-screen):** `html,body{overflow-x:hidden}` added as a global safety net (was missing entirely — any single overflowing child could scroll the whole page sideways). Root causes fixed rather than just masked: (1) the captain's-log "Distans" row had a fixed-content button (`#clogFillMiles`) next to a `flex:1` input with no `flex-wrap` — classic mobile trap, flex items don't shrink below content size by default; added `flex-wrap:wrap`+`min-width:70px`. (2) The Uno score `<table>` had no `table-layout:fixed`, so a long player name could grow the table past the viewport; fixed with `table-layout:fixed` + explicit column widths. (3) `#clogFrom`/`#clogTo`/`#clogWeather`/`#clogNotes` had no `maxlength` (every other text input in the app does) — added 40/40/60/300. (4) `.cpCard .t` (used for harbor-swap cards and log entries, both with a `float:right` badge) got `overflow-wrap:anywhere` so long unbroken text can't force width. Verified empirically with Playwright + real Chromium at 320px and 390px viewports — every tab, the boat-creation flow, and a deliberate stress test with an unbreakable 50+ char boat name, all checked via `scrollWidth` vs `clientWidth`: zero overflow after the fix (jsdom can't catch this class of bug at all, since it doesn't run CSS layout — worth remembering for future layout work).
- **More tap/switch animation polish (added 2026-07-14):** the app already had a lot of juice (wave-sweep tab transitions, confetti, floating +points, board ship bob, current-tile pulse ring, splash sail animation, `prefers-reduced-motion` respected globally) — this pass filled concrete gaps rather than re-doing what existed. `.arcBtn`, `.sailorBtn`, `.emojiRow button`, `.padRow button` all had the raised "3D button" drop-shadow look implying press-ability but **zero `:active` press feedback** — tapping did nothing visually. Added matching press-down transforms to all four. Added `.cpCard.tapCard:active{transform:scale(.97)}` scoped only to the genuinely-clickable planning-mode swap cards (deliberately did *not* add whole-card press feedback to the default `.cpCard`, since most instances — role rows, captain-log entries — have buttons *inside* them rather than being clickable themselves; a card-wide press effect there would misleadingly suggest the whole card is tappable). The crew-points coin (`#hudCrew`) now pulses on increase, mirroring the existing personal-points pulse (`_lastCrewPts` tracking added to `updateTop()`, same pattern as `_lastPts`). Tab switching gets a keyframe bounce (`tabHop`/`tabIcPop`) instead of an instant snap — needed a `@keyframes`-based approach rather than `transition`, since `buildTabs()` destroys and recreates the tab elements every render, so there's no persisting element for a CSS transition to interpolate from. Verified via Playwright: transitions present on all four button classes, tab bounce animation applied, crew-coin pulse fires on a real score change, and — importantly — everything correctly goes to `0s`/none under `prefers-reduced-motion: reduce` via the pre-existing global media query.
- **Mistral cloud fallback for @gemma (added 2026-07-14):** `netlify/functions/mistral-fallback.mts`, a Netlify **Scheduled Function** (`schedule: "*/2 * * * *"`, min interval Netlify allows is 1 min) that runs independently of Roope's home PC — this is the key difference from just adding a fallback *inside* `gemma_bridge.py`, which was considered and rejected: if the fallback lived only in the Python bridge, it would never run when the PC that hosts it is off, defeating the point. The function polls the same `chat` table (same duplicate-detection via `reply_to`, same system prompt/persona as the Python bridge) and only answers an `@gemma` question if it's been unanswered for **3+ minutes** (`GRACE_MS`) — long enough that the local Gemma bridge, when the PC *is* on, always gets first crack at it (it normally replies within ~15s) — and gives up on anything older than 30 minutes (`STALE_MS`), so it won't dredge up ancient unanswered questions. Answers post under the same `"⚓ Gemma"` author for a seamless crew experience (no visible distinction between "home Gemma" and "cloud Mistral" answers — a deliberate simplicity choice, not an oversight). Uses `mistral-small-latest`.
  - **Security/cost note:** the Mistral API key is read via `Netlify.env.get("MISTRAL_API_KEY")` — set only in Netlify's project environment variables (Site configuration → Environment variables), never committed to this repo or shipped to the public site. This matters more here than the Supabase anon key situation: an exposed *paid* third-party API key on a publicly reachable site (no real auth beyond PIN) would be a genuine abuse/cost-exposure risk, unlike the Supabase key which is protected by RLS and already an accepted trust tradeoff. **The key has not been set yet** — until someone adds it via the Netlify dashboard, the function safely no-ops every run (checked and logged, doesn't error) and the game works exactly as before with only the home-PC Gemma. Setup steps are in `SETUP.md`.
  - **TEMPORARY change (2026-07-14, same day):** `GRACE_MS` dropped from 3 min to 5 s and the schedule tightened from every 2 min to every 1 min (Netlify's minimum), on request — real chat data showed 3-day-old `@gemma` questions still unanswered by the home bridge, so it's effectively Mistral-primary right now rather than a true fallback. **Cannot actually "turn off" `gemma_bridge.py` from here** — it's a script on Roope's physical machine with no remote control surface; if it's not running, there's nothing to turn off, and if it ever *is* running again, it'll still occasionally win the race on a 5s grace window (harmless, just a nice bonus, not a conflict — duplicate-answer protection via `reply_to` still applies). To restore "wait for local Gemma first" behavior later, bump `GRACE_MS` back to `3*60*1000` (or similar) and the schedule back to `"*/2 * * * *"` — both are called out with inline comments in the file.
  - **Confirmed working live (2026-07-14):** verified end-to-end against the real Supabase `chat` table, not just mocked tests — Roope's real question "hur långt är det till nådendal?" got a real, coherent, in-character Mistral answer ("⚓ Gemma", ~25 nm / 5h at 5kn, with the expected sjömanshumor) in **~57 seconds** from when the correctly-keyed deploy went live. Root-caused an earlier false alarm (7 min with no answer) to env vars only taking effect on the *next* deploy after being added, not retroactively on already-running functions — worth remembering for any future env var troubleshooting here.
- **Diskat idag? / dishes chore points (added 2026-07-14):** small addition to the 🎁 Dagens tab, same visual pattern as the role-task card — `DISH_PTS=2`, `kind='bonus'`, `ref='dishes:<date>'`. Deliberately **not** role-gated (unlike role tasks) — anyone aboard can grab it, once per person per day, no captain/role assignment needed. No schema change; reuses the `bonus` kind and the `one_per_sailor` unique index that already covers it (added for roles). Verified via jsdom: card renders, logging awards +2 p and flips to the done state, button disappears after use.
- **"Diskmästaren" leaderboard in Törnrapport (added 2026-07-14):** counts each sailor's `dishes:*` bonus logs (`dishCount()`, filters `ref.startsWith("dishes:")` — distinct from role-task bonus logs which use a `role:` prefix, both share `kind='bonus'`). Adds a small standalone 🍽️👑 card near the top of the report naming whoever has the most (ties shown together, e.g. "🐟 Roope & 🏴‍☠️ Pati"), only rendered if at least one sailor has logged dishes, plus runner-ups below in a dimmed list. Also added a "🍽️ Diskat" count row to each sailor's existing per-person stat card. Verified via jsdom with 2 sailors at 3 vs 1 logged days: champion card shows the right person/count, per-sailor rows show correctly.
  - Verified by running the actual `.mts` file (not a reimplementation) via `node --experimental-strip-types` with mocked `fetch`/`Netlify.env`: confirmed it correctly answers only a stale (4-min-old) unanswered question, and correctly skips a too-fresh (1-min) one, an already-answered one, a 40-min-stale one, and a non-`@gemma` message — plus a separate run confirming the graceful zero-op behavior when `MISTRAL_API_KEY` is unset. Could not test the actual Netlify Scheduled Function runtime/cron trigger itself from the sandbox (no Netlify CLI/runtime available here) — worth a real smoke test once a key is configured and deployed.
- **New-boat setup wizard + per-boat trivia bug fix (added 2026-07-15):** requested as "an initial travel wizard when a new boat is set up during mönstring." Two parts:
  - **Real bug fixed:** a single hardcoded `const BOAT={...}` held Sun-Day's specs (model/LOA/beam/draft/mast/weight/rig) and was used both for the Sjötermer boat diagram and for 8 hardcoded trivia questions (3 in `QUIZ`, 5 in `POPQUIZ`) — meaning **every boat's crew, including `Vänbåten`, was quizzed on Sun-Day's numbers.** Replaced with per-boat spec columns on `boats` (`model`/`loa`/`beam`/`draft`/`mast`/`weight`/`rig`, all `text`, all nullable) and `myBoatSpecs()` (reads the viewer's own boat, gracefully returns `null` for any unset field). Sjötermer (`renderTerms()`) and the boat SVG diagram (`sloopSVG()`, which also had "Sun-Day" hardcoded into its `aria-label` and in-diagram text) now read the viewer's actual boat name/specs, with fallback text ("Båtens mått är inte inlagda ännu.") when a boat has no specs set — this matters because `Vänbåten` predates this feature and has no specs, by design (no retroactive-edit UI was built).
  - **Dynamic per-boat trivia:** the 8 hardcoded Sun-Day questions were deleted from `QUIZ`/`POPQUIZ` and replaced with `boatSpecQuestions()`, which generates up to 6 multiple-choice questions (model/draft/mast/beam/weight/rig) **only for specs the viewer's boat actually has set** — skips silently otherwise, so a spec-less boat like `Vänbåten` gets zero boat-spec questions rather than blanks or errors. Distractors are drawn from fixed pools (`BOAT_MODEL_POOL`, `BOAT_LOA_POOL`, etc., 4-8 plausible values each) via `pickDistractors()`/`mcQuestion()` — a deliberate design choice (confirmed with the user rather than assumed) to use dropdown-select pools instead of freeform text inputs in the wizard, specifically so distractor generation never has to parse arbitrary user-typed strings with mixed units/formatting. `allQuiz()`/`allPopQuiz()` (== static array + `boatSpecQuestions()`) replace the old direct `QUIZ`/`POPQUIZ` references in `startQuiz()`/`maybePopQuiz()`.
  - **The wizard itself:** the "➕ Ny båt" branch of the mönstring form (`newSailorForm()`, `#nsNewBoatFields`) now has, beyond the existing name+emoji: a starthamn `<select>` (defaults to "Helsingfors (Katajanokka)", stored as the literal `"HELSINGFORS"` to match the existing special-case in `harborCoords()`) and a målhamn `<select>` (defaults to "Nådendal (Naantali) — Målgång!", stored as the literal `"NÅDENDAL"` to match the existing `HARBOR_CH` key and keep the celebratory finish-line flavor text) — both pull their non-default options from `allHarborNames()` (dedup+sorted union of `PLACES`+`ALT_HARBORS`+`EXTRA_HARBORS` names), confirmed with the user as "pick from an existing list" rather than freeform text. Plus 7 boat-spec `<select>` pickers (model/loa/beam/draft/mast/weight/rig), each pool-backed with a blank "— välj —" default (so skipping a field stores nothing rather than silently picking the pool's first option) and an "Annat (skriv själv)" escape hatch that reveals a freeform text input (`specFieldHtml()`/`wireSpecField()`/`specFieldValue()`) for boats that don't match any pool value. Per the user's explicit choice ("default route, edit later via Planeringsläge"), the wizard does **not** ask for the 13 middle stops — `route` is computed as `[...DEFAULT_ROUTE.slice(0,13), chosenEndHarbor]`, and the existing Planeringsläge harbor-swap picker remains the way to customize the middle stops afterward. `.field select` had no CSS rule before this (the app had never used a `<select>` anywhere) — added alongside `.field input`.
  - **Threaded the boat's own start harbor through everywhere `"HELSINGFORS"` was hardcoded:** `setRoute()` now takes an optional `startHarbor` param (`TILE_SHORT=[startHarbor||"HELSINGFORS",...ROUTE]`), `applyMyBoatRoute()` passes `b.start_harbor`, and the "other boats" comparison line in `renderBoard()` was also fixed (`[x.start_harbor||"HELSINGFORS",...x.route]` instead of always `["HELSINGFORS",...x.route]`) — this was a second, smaller instance of the same Sun-Day-only-assumption bug pattern, caught while auditing every `TILE_SHORT`/`"HELSINGFORS"` reference.
  - Supabase migration applied live: `alter table boats add column start_harbor/end_harbor/model/loa/beam/draft/mast/weight/rig text` (all nullable), then backfilled Sun-Day's row with its real specs (`start_harbor='HELSINGFORS'`, `end_harbor='NÅDENDAL'`, `model='Degerö 33'`, etc.) so its Sjötermer/trivia content is unchanged from before this feature. `Vänbåten` was left with all 9 fields `NULL` (predates the wizard). `schema.sql`'s `boats` table and seed `insert` updated to match.
  - Verified via a new jsdom test (`test_boat_wizard.js`): wizard fields render on the new-boat form; a boat created with custom specs shows those specs (not Sun-Day's) in Sjötermer and gets boat-spec trivia questions that reference it by name; a boat created with a custom start/end harbor gets the right `route`/`start_harbor`/`end_harbor` and the board caption reflects the custom start harbor; a boat created with zero specs filled in degrades gracefully (fallback Sjötermer text, no boat-spec quiz questions, no crash) — plus the full existing regression suite (`test.js`–`test6.js`, `test_dishes.js`, `test_dishking.js`) re-run clean.
- **⚙️ Kaptensrutan — consolidated captain settings page (added 2026-07-15):** follow-up request after the boat wizard: "add [a boat] rename option for the captain, but I also think [the] captain needs [their] own settings/overview page." User picked all four proposed contents (boat details, crew roles, route/Planeringsläge, transfer captaincy) and placement as a new tab in the Mer sheet. New nav entry `["kapten","⚙️","Kaptensrutan"]` in `NAV_MORE`, `renderKapten()` renders into `#kaptenBox`.
  - **Visible to everyone, editable by the captain only** (per the user's explicit choice): non-captains see a read-only summary (boat emoji/name, specs line, start/goal harbor, current captain, read-only role-holder list, a "Bli kapten" button if the boat has no captain yet). The captain sees the full edit form instead.
  - **Boat details, now editable after creation (the rename ask):** name, emoji, start/goal harbor `<select>`s, and all 7 spec fields — the exact same picker UI as the "Ny båt" wizard, reused via `specFieldHtml()`/`harborSelectHtml()` now generalized to take a `prefix` param (`"ns"` for the mönstring wizard, `"kap"` here) and an optional `current` value to pre-select/pre-fill from the boat's existing data, including falling into the "Annat" freeform slot if a stored value doesn't match any pool option. One "Spara båtuppgifter" button persists name/emoji/harbors/specs together (`saveBoatDetails()`); renaming re-checks the `boats.name` unique constraint against other boats (excluding itself) client-side before saving, matching the same check the wizard already does at creation time. Editing the goal harbor only replaces the last slot of the existing `route` array (`[...route.slice(0,13), newEndHarbor]`) — it does not touch the 13 middle stops, so any prior Planeringsläge customization survives. After a save, `setRoute()`/`refreshWhoAmI()` are called explicitly so the new name/start-harbor take effect immediately in the current session (`TILE_SHORT[0]`, the `#whoami` header) without needing a reload — `refreshWhoAmI()` is a small new extraction from what `enterApp()` used to do inline, so it can be called again after an edit.
  - **Crew role assignment — moved, not duplicated:** the picker (`renderRoleAssignPanel()`, extracted from what used to be inline in `renderSur()`) that lets the captain assign the 6 roles to crew now lives only in Kaptensrutan; it's gone from the 🎁 Dagens tab (which was "a bit buried," per the user's own framing of the problem). Dagens still shows "Din roll idag" (today's task + the log button) for whoever holds a role — that's role *use*, not role *assignment*, and stayed put.
  - **Route/Planeringsläge — folded in, not duplicated:** the "🧭 Planeringsläge" button is gone from the board tab (`renderBoard()`); an "🧭 Öppna Planeringsläge" button in Kaptensrutan opens the exact same `routeEditor()` overlay as before (wind forecast, next-stop ETA, harbor swap picker) — none of that logic was rewritten, just re-triggered from a new place. **Gated on `canPlan()` (captain OR navigator), not `isCaptain()`** — this one caught a real regression during testing: an early version only showed the button inside the captain-only edit branch, which would have taken away the navigator's existing ability to open Planeringsläge (previously available via the board button to anyone with `canPlan()===true`). Fixed by rendering the route section independently of the captain/non-captain branch, keyed on `planner=canPlan()` instead.
  - **Transfer captaincy (the fourth requested piece, previously not possible at all):** `claimCaptain()` was refactored into a shared `setCaptain(name,toastMsg)` helper; `claimCaptain()` now calls `setCaptain(me.name,...)` and a new `transferCaptaincy(sailor)` calls `setCaptain(sailor.name,...)`. Kaptensrutan lists every crew member on the boat with a "Gör till kapten" button (hidden for whoever already holds it) — since captaincy is matched by name string (`boats.captain`, case/space-insensitive, same mechanism as before), transferring is just overwriting that string, no schema change needed.
  - Verified via a new jsdom test (`test_kaptensrutan.js`): board tab no longer has `#routeEd`, Dagens no longer has the role-assignment list, captain's Kaptensrutan shows a prefilled editable name field plus role/route/transfer sections, renaming a boat updates both the stored boat record and the `#whoami` header live, Planeringsläge still opens correctly from its new location, a non-captain gets the read-only view with no edit fields, and transferring captaincy flips `isCaptain()` for both the old and new captain correctly. Also had to update three pre-existing tests (`test2.js`, `test5.js`, `test6.js`) whose assertions pointed at the old board-tab/Dagens-tab locations for role-assignment and route-editor UI — not regressions, just relocated UI the old tests hadn't been told about yet; full suite re-run clean afterward.
- **🎣 Fiske & Handel / "Skärgårdshandlaren" — new fishing-and-trading minigame (added 2026-07-15):** user asked for a brand-new archipelago-themed game — fish and gather, sell the catch, use the money to buy fuel and boat upgrades (better sails, a fridge, etc.). Brainstormed as a pitch first, then scoped via two explicit choices: **shared per-boat economy** (not per-device) and **a small capped real-trip-points bonus** on top of the new kronor currency (both user-picked "Recommended" options). New nav entry `["trader","🎣","Fiske & Handel"]` in `NAV_MORE`, its own tab with 4 internal sub-screens switched via a `.catChips` segmented control (`traderTab` state, reusing the same chip pattern as the 🔭 Spana category switcher).
  - **Data model:** new `boats.workshop jsonb not null default '{}'::jsonb` column (migration applied live, `schema.sql` updated) holding `{kronor, fuel, rodLevel, fridge, sails, net, netAt, catch:[{k,qty,day}]}` for the whole boat — shared by the whole crew the same way `boats.roles`/`boats.route` already are (read-modify-write on `myBoat()`, no new realtime channel, "eventually consistent on next load" — consistent with the existing trust model and how role/route edits already behave for other crew members). `myWorkshop()` reads with defaults merged in; `saveWorkshop()` persists (localStorage in LOCAL_MODE, `SB.from("boats").update()` otherwise).
  - **🎣 Fiske (fishing):** costs 1 bensin (fuel) to start a ~60s session (+25s with Bättre segel). Within a session: "Kasta ut" → a random 0.7–1.8s bite delay → a strike-bar minigame appears (an orange marker sweeping a bar via a pure triangle-wave function of elapsed time, `castPosition()`) → "Dra upp!" reads the marker's position *at click time* and resolves hit/miss/perfect (`resolveCast()`). A wider rod (`strikeZoneWidth()`, gated by `rodLevel` 0–3, `ROD_NAMES`/`ROD_COSTS`) makes the zone easier to hit and unlocks rarer species (`FISH_SPECIES`, weighted random via `rollSpecies()`, `minRod` gates strömming/abborre → gädda → torsk/lax). Catching the rare lax grants a real capped trip-points bonus (`FISH_PTS=3`, `kind='bonus'`, `ref='fish:<date>'`, once/sailor/day — same `one_per_sailor` unique-index pattern as dishes/role tasks, no new index needed) plus confetti.
  - **🦪 Samla (gather):** free, no fuel, no timing skill — a single "Leta vid stranden" button with a ~1s "Letar…" delay and an 80% chance of finding blåmusslor/bär/kräftor (`GATHER_ITEMS`). Deliberately the "always something to do to bootstrap money" on-ramp, distinct pacing from Fiske so there's a free option even at zero fuel/zero kronor.
  - **🏪 Marknaden (market):** sells the shared catch. Prices rotate **daily and deterministically** per species (`dailyPriceMult()`, same date-hash pattern as the existing daily quiz/surprise rotation) so "today's prices" are consistent for the whole crew without needing a server round-trip. Spoilage (`applySpoilage()`): any catch not sold by the next calendar day is discarded automatically on next Marknaden visit — *unless* Kylskåp (fridge) is owned, which is the whole point of that upgrade.
  - **⚓ Verkstaden (workshop):** spend kronor on ⛽ Bensin (`FUEL_PACK`=5 units per purchase), 🎣 rod tiers, ❄️ Kylskåp (stops spoilage), ⛵ Bättre segel (+25s fishing sessions), 🕸️ Nät (passive income — `netAccrued()` computes kr accrued since `netAt`, capped at `NET_CAP_HOURS`×`NET_RATE`=24 kr so it can't be left to farm indefinitely offline; a "Töm nätet" button collects it).
  - Built for testability: the core timing/economy logic is pure top-level functions (`castPosition`, `resolveCast`, `strikeZoneWidth`, `rollSpecies`, `dailyPriceMult`, `sellPrice`, `addCatch`, `applySpoilage`, `netAccrued`) independent of the `setInterval`/`setTimeout`-driven UI animation, so the actual hit/miss/pricing/spoilage math is unit-tested with exact synthetic inputs rather than fighting real timers in jsdom. Verified via a new `test_trader.js`: all pure functions directly (including a statistical check that `rollSpecies` never returns a rod-gated species below its `minRod`, and does return the rare species often enough across 300 rolls at higher rod levels), the full purchase/sell/net-collect economy flow, that two different crew members logging into the *same* boat see the *same* shared kronor/fuel/upgrades (the core "shared economy" requirement), and a UI smoke test driving one real fishing session end-to-end (start → cast → wait for a real bite → reel in) plus the Samla and Verkstad screens via actual DOM clicks. Also visually verified with Playwright + real Chromium at a 390px mobile viewport (`trader_visual_check.js`) — no horizontal overflow on any of the 4 screens, strike bar and cards render as intended.
- **Fiske & Handel reworked into a walkable village scene (same day, 2026-07-15, follow-up):** two fixes requested after first shipping the tab-based version above. (1) **Moved into 🕹️ Spelrum as a 6th arcade tile** (`["trader","🎣","Fiske & Handel","Fiska, samla & handla"]` in `renderArcade()`'s game list) instead of a separate `Mer` entry — it's a game, so it belongs with Tetris/Memory/Match-3/Ålen/Uno-poäng. `startTrader(box)` now mounts into `#arcadeBox` like the other games; the `NAV_MORE`/`VIEW_IDS`/`render()` entries for the old standalone `trader` tab were removed. (2) **Replaced the `.catChips` tab UI with an actual walkable top-down scene** — the user's vision: walk across the beach to fish small fish or gather, walk to the bryggan to take the boat out for bigger fish, walk into the village for the market and an outdoor workshop. Scoped via two more explicit choices: **tap-to-walk** movement (not a D-pad) and **two fish-pool tiers** (beach = free, common fish only; boat = costs fuel, unlocks the full rarer/bigger pool) — both user-picked over the alternative option offered.
  - **The scene:** a single CSS-gradient background (sky/sand/dock/grass, `.traderScene`) with 4 tappable landmarks (`TRADER_LANDMARKS`: 🏖️ Stranden, ⚓ Bryggan, 🏪 Marknaden, ⚒️ Verkstaden, each with fixed `{x,y}` percent coordinates) and a player avatar rendered as **the sailor's own emoji** (`me.emoji`), absolutely positioned and CSS-transitioned (`transition:left .5s,top .5s`) so movement glides rather than snaps. Tapping a landmark icon or any open point on the scene calls `traderWalkTo(x,y,onArrive)`, which updates the avatar's `style.left/top` directly (not via an innerHTML rebuild, so the CSS transition can actually play) and schedules `onArrive` via `setTimeout` at a duration scaled to walking distance (250–900ms) — landmark taps pass a callback that opens that landmark's panel on arrival; open-water taps just walk with no callback (free exploration).
  - **Persistent scene, re-rendered panel:** `startTrader(box)` builds the scene markup (landmarks + avatar) **once** per arcade-game activation — critical because the rest of the app's convention is "rebuild everything via `innerHTML` on every render," which would destroy the avatar element and kill the walking animation on every state change. Only a dedicated `#traderPanel` child div is rebuilt when a landmark's content changes (`renderTraderPanel()`, dispatching on `traderPanelKind`); the scene itself is never touched again until the arcade tile is reselected (matches how every other arcade game already resets fresh on reselection, so this isn't a new inconsistency).
  - **Beach → chooser → Fiske (free, small fish) or Samla:** walking to the beach opens a small chooser card (`renderBeachPanel()`, `traderBeachMode` sub-state) offering "🎣 Fiska (gratis)" or "🦪 Samla på stranden". Beach fiske reuses the exact same strike-bar minigame as before but rolls against a new, smaller `BEACH_FISH_SPECIES` pool (strömming/abborre only) and never touches `fuel`.
  - **Bryggan → boat trip (costs fuel, full/rare pool):** walking to the dock shows a "⚓ Bryggan" card; "Åk ut och fiska (−1 ⛽)" deducts fuel and starts the same fishing session but rolling against the full `FISH_SPECIES` pool (gädda/torsk/lax included, still `rodLevel`-gated) — this is the mechanical payoff for "take the boat out for bigger fish." `startFishSession()` now takes a `mode` ("beach"|"boat") threaded through to `reelIn()`'s species roll.
  - **Marknaden/Verkstaden:** same panel content and purchase logic as before, just triggered by walking to their landmark instead of tapping a chip, each with a "⬅ Gå vidare" button that closes the panel (`traderShowPanel(null)`) so the player can keep exploring.
  - Verified via an updated `test_trader.js` (landmark taps replace chip clicks; new checks that the avatar shows the sailor's own emoji, that beach fiske costs no fuel while boat fiske via the bryggan does, and that tapping open water with no real jsdom layout doesn't crash) — full pure-function/economy/shared-boat coverage from the original build carried over unchanged since none of that logic moved. Also re-verified visually with Playwright + real Chromium (`trader_scene_visual.js`): scene, avatar, and all four panels render correctly with zero horizontal overflow at 390px, including a real pixel-coordinate tap-to-walk (not just landmark-button taps) exercising the `getBoundingClientRect()` math that jsdom can't validate.
- **Crew-submitted species in 🔭 Spana (added 2026-07-15):** requested as "add feature to be able to add things to spana - name+group." New `custom_spots` table (`name`, `grp` — one of the `GAMES` category keys, `author`, `created_at`), loaded at boot alongside boats/sailors (`loadCustomSpots()`) and refreshed by the existing "Uppdatera" button. `itemsFor(catKey)` merges `GAMES[catKey].items` with the matching custom entries (`customSpotsFor()`) and is now used everywhere `renderSpotGrid()` previously read `g.items` directly — custom entries are always appended *after* the fixed pool so their index-based log ref (`curCat+":"+i`) stays stable across renders. New "➕ Lägg till art" toggle button above the category chips opens a small inline form (name + a `<select>` of the 6 categories); submissions default to `v:1` (common) with no rarity flag, attributed to whoever added them ("Tillagd av `<name>`" shows as the card's tip text) — kept intentionally minimal (no photo, no moderation/delete UI, no rarity picker) since the ask was just "name+group." Custom items are fully loggable for real points exactly like the built-in ones. Verified via `test_spota_ideas.js`: form validation, the new card appears with the right name in the right category only, survives navigating away and back (confirms it's persisted, not just in-memory), and logging it awards points normally.
- **💡 Idélåda — feature/idea suggestion box (added 2026-07-15):** requested as "somewhere add also feature that request feature or new idea like for game or other." New `["ideas","💡","Idélåda"]` tab in the Mer menu, backed by a new `ideas` table (`author`, `body`, `created_at`). Deliberately minimal by design — a textarea + "Skicka idé" button and a reverse-chronological list of everyone's submissions with author + date, no categories, voting, or status tracking, since the ask was just a shared place to drop ideas, not a full feature-tracker. Same load-on-nav pattern as Galleri/Chat (`loadIdeas()` then re-render for freshness). Verified via `test_spota_ideas.js`: empty-state message, validation on empty submission, submitted idea appears with correct attribution, and persists across navigation.
- **Frågesport daily-variation, for the record (asked 2026-07-15, no code change):** the bookable 🎯 Frågesport tab (`startQuiz()`/`renderQuestion()`) draws from a **static** question pool (`QUIZ` + `boatSpecQuestions()`) — replaying it shuffles the *order* but never changes the *content*; only the points you can bank are capped once/day (`ref="quiz:"+today()`). The separate **daily pop-quiz overlay** (`maybePopQuiz()`, the one that pops up automatically, +2 p) *does* genuinely change day to day — it deterministically picks one question by a hash of the date from the combined `allPopQuiz()` pool, so everyone sees the same single question on a given day and a different one tomorrow.
- **Fiske & Handel village scene upgraded to real WebGL 3D (2026-07-15, follow-up):** user shared a reference image of a painterly isometric village and asked for "more advanced graphics… a bit 3d." Given a three-way scope choice (lightweight CSS/SVG depth polish vs. a static illustrated background image vs. real WebGL/three.js), the user picked **real 3D** despite my flagging it as the biggest lift and a step away from the app's "single lightweight file" pattern so far — so it shipped as asked.
  - **Library:** added `three@0.160.0` via the same CDN-`<script>` pattern the app already uses for Supabase and Leaflet (`<script src="https://cdn.jsdelivr.net/npm/three@0.160.0/build/three.min.js">`, right after the Leaflet include). No build step, no bundler, consistent with everything else in this file.
  - **Scene:** `initTraderScene()` builds a real `THREE.Scene` on an `OrthographicCamera` (flat, no-perspective-distortion "cute isometric" look) with ambient + shadow-casting directional light, three flat colored terrain slabs (water/sand/grass, matching the app's existing color palette), a dock, and low-poly landmark meshes built from primitive geometry only (cones, boxes, cylinders — no external 3D model files, so there's nothing to host or fail to load beyond the one three.js script): a beach umbrella, a moored sailboat at the dock, a market building with a pyramid roof, and a workshop shed with a barrel. The player's avatar is a small stylized sailboat mesh that glides between spots via a `requestAnimationFrame` lerp loop (`traderAnimate()`), with a subtle idle water-bob animation running continuously while the game is open.
  - **Interaction stayed deliberately hybrid, not fully rewritten:** landmark tap-targets are still the same HTML `.tSpot` buttons as the 2D version (`#tSpot-beach` etc.) — only their position changed, from hand-picked percentages to values computed *once* via a 3D→screen projection (`traderWorldToScreenPct()`) right after the camera is set up, since the camera never moves. This was a deliberate risk-reduction choice: it means landmark clicks, the whole panel system (Fiske/Samla/Marknad/Verkstad, the fishing minigame, the shared economy) needed **zero changes** — only `traderWalkTo()` (now animates a 3D mesh position instead of a CSS `left`/`top` transition) and `startTrader()`/scene setup actually changed. Free-form tap-to-walk on open ground (not a landmark) is the one interaction that does use real 3D raycasting (`THREE.Raycaster` against a ground plane), since that has no fixed screen position to precompute.
  - **Graceful fallback:** everything is wrapped in `if(window.THREE)` — if the three.js CDN script fails to load (offline, slow connection, or a future CDN change), `startTrader()` falls back to the exact same simple percentage-based landmark positions as the pre-3D version instead of crashing, so the game stays playable (just visually flat) rather than breaking.
  - **Cleanup:** `stopTraderScene()` (new) disposes the WebGL renderer and cancels the animation frame loop — wired into `renderArcade()` (both when switching to a different arcade game and unconditionally at the top before re-selecting Fiske & Handel, matching the existing "every game restarts fresh" convention), into `render()`'s arcade-tab-exit path, and into `logout()`, so repeatedly switching games or logging out doesn't leak WebGL contexts (browsers cap the number of live contexts per page).
  - **Verification note — real limitation, documented rather than glossed over:** this coding sandbox's outbound network policy returns a 403 on `cdn.jsdelivr.net` and `unpkg.com` (confirmed via the proxy's own status endpoint: `"gateway answered 403 to CONNECT (policy denial or upstream failure)"`), so Playwright running *inside this session* cannot load three.js (or, incidentally, Leaflet — a pre-existing constraint, not something this change introduced) to take a real screenshot directly against the shipped CDN reference. Worked around this **for verification only** by `npm install three@0.160.0` (npm's registry is separately allowlisted) and pointing a scratch copy of the page at the local file instead of the CDN — confirmed via that local copy that the scene renders correctly (terrain, shadows, fog, landmark meshes, the boat avatar, tap-to-walk, and landmark→panel navigation all work, screenshotted at a 390px mobile viewport with zero horizontal overflow). The shipped `index.html` itself still references the real CDN URL, unchanged — production already depends on `cdn.jsdelivr.net`/`unpkg.com` successfully for Supabase and Leaflet, so there's good precedent it'll load fine for real users; this sandbox's proxy restriction is specific to this dev session, not a property of the deployed site. Full jsdom regression suite (all `test_*.js` files) still passes clean, since jsdom never executes external `<script src>` tags regardless (confirmed empirically) and the `if(window.THREE)` fallback path is exactly what jsdom exercises.
- **Fiske & Handel: painterly environment pass + RSL-style HUD chrome (2026-07-15, same-day follow-up):** user shared a second reference (a colorful painterly isometric village — waterfall, signpost, windmill house) and asked to combine it with the earlier RSL-style mobile-RPG hub screenshot (resource bar, numbered badges on locations, a bold red "BATTLE" button) as inspiration, plus asked specifically what action should sit behind a BATTLE-equivalent button.
  - **Environment (image 1 inspiration):** `initTraderScene()` got warmer/richer: a `THREE.HemisphereLight` (sky-to-ground gradient light) replaced flat ambient light for more natural coloring, terrain colors brightened, two low background hills (`traderBuildHill()`) added for depth near the water horizon, ~7 low-poly pine trees (`traderBuildTree()`, stacked cones + trunk) scattered across the grass, 14 small colored "flower" dots for color pop, and 3 simple puffy cloud clusters (`traderBuildCloud()`) in the sky. Buildings got small character details — a flag on the market roof, a chimney on the workshop — matching the reference's decorated-building feel. Deliberately did not attempt the reference's literal waterfall/bridge/hot-air-balloon (diminishing returns for a background scene vs. the effort, and this app's landmarks already carry that role functionally).
  - **HUD chrome (image 2 inspiration), answering "what's behind the battle button":** added a **`🎣 FISKA` CTA button** (`.traderCta`, bold red/orange pill, bottom-right of the scene, same visual weight as the reference's BATTLE button) that's the direct answer to the user's question — tapping it opens `renderQuickFishPanel()`, a chooser between "🏖️ Stranden — små fiskar (gratis)" and "⛵ Båten — större fisk (−1 ⛽)" (disabled if no fuel), and picking either **jumps straight into the fishing minigame immediately** (calling `startFishSession()` directly) rather than requiring the player to walk there first — a fast-path shortcut layered on top of the existing walkable exploration, not a replacement for it (the beach/bryggan landmarks still work exactly as before for players who want to wander). Also added small **red count badges** (`.tBadge`, `traderBadgeHtml()`) on the Marknaden landmark icon (shows unsold catch count) and Verkstaden (shows an "!" when passive nät income is ready to collect) — mirroring the reference's numbered location badges — refreshed on every `traderUpdateHud()` call so they stay live as the game state changes. The kr/fuel resource bar was restyled from a single flat card into two separate rounded pill chips (`.traderHud`/`.traderChip`) closer to the reference's resource-pill look; caught and fixed a real bug in the same pass — the new chips had no explicit text color and were invisible (inheriting the app's light body-text color onto a light pill background) until Playwright screenshots caught it, fixed by setting `color:var(--ink)` explicitly.
  - Verified via updated `test_trader.js` (CTA button existence, quick-fish chooser opens with both options, picking beach starts a session with no manual walk required, Marknaden badge appears when catch is unsold) and the same local-three.js Playwright screenshot workflow as the previous 3D entry (390px viewport, zero overflow, badge and CTA button visually confirmed, chip-text-invisibility bug caught and fixed before shipping).
- **Fiske & Handel reverted from 3D back to a literal illustrated background (same day, 2026-07-15, final follow-up):** the 3D/WebGL scene above was a misread of the ask — the user's actual request ("O i meant copy the [i]tal image as the background and add cta buttons") was to use a *reference image itself* as the background, not build abstract 3D primitives inspired by it. When asked to clarify scope (generate a new image vs. reuse the exact upload; keep or drop walking), the clarifying question was explicitly declined — the signal was to just act on the two supplied images directly rather than re-confirm. The user then supplied a second, purpose-made image (`fiske-village.jpg`, checked into the repo alongside `index.html` — same pattern as `boat.jpg`) that already has a painted wooden signpost with four arrows labeled **TORG / VERKSTAD / BRYGGA / STRAND**, matching the game's four existing destinations exactly, plus a sailboat "SUN RAY," a lighthouse, cottages, a market stall, a boathouse, and "HOLME"/"VASS" signs — a perfect thematic fit needing no generation or reconciliation.
  - **Everything 3D was torn out**, not just hidden behind a flag: `TRADER_LANDMARKS_3D`, `traderThree`, `initTraderScene()`, `traderAnimate()`, `traderRender()`, `traderBuildLandmarkMesh/Tree/Hill/Cloud()`, `traderWorldToScreenPct()`, `traderWalkTo()`/`traderGoTo()`, the raycasting tap-to-walk handler, `stopTraderScene()`, and the quick-fish CTA/chooser (`renderQuickFishPanel()`, `.traderCta`) are all gone, along with the now-unused `<script src=".../three@0.160.0/three.min.js">` CDN tag. The three call sites that used to dispose the WebGL context (`logout()`, `render()`'s arcade-tab-exit path, `renderArcade()`) had their `stopTraderScene()` calls removed too, since there's no more animation-frame loop or GL context to clean up.
  - **New model: static image + 4 direct-navigation buttons, no walking, no avatar.** `TRADER_SPOTS` is a flat array of `{k,label,x,y}` percent-positioned entries; `startTrader(box)` renders them as `.tSpot` buttons directly onto `#traderScene` (a `<div>` with `background:url('fiske-village.jpg') center/cover`, no `<canvas>`) and each button's `onclick` calls `traderShowPanel(k)` synchronously — no walk animation, no arrival callback, no raycasting. This is a large simplification: `renderTraderPanel()`, `renderBeachPanel()`, `renderBryggaPanel()`, and everything downstream (`renderMarknadPanel`, `renderVerkstadPanel`, the fishing minigame, the shared-boat economy) needed **zero logic changes**, only the entry point.
  - **Button styling had to be iterated after an initial visual bug:** the first pass used centered, stacked (flex-column icon-over-label) chips at percentage coordinates eyeballed from a description of the image, not measured against it — a Playwright screenshot showed them badly overlapping (42px-tall buttons spaced only ~22px apart) with label text hidden underneath the next button. Fixed by (a) actually reading the shipped `fiske-village.jpg` file to locate the real signpost position (it's lower-left, roughly y:63–93% of the image height, not the initially-guessed y:38–80%), and (b) redesigning `.tSpot` as compact horizontal pills (icon+label side-by-side, ~25px tall, left-anchored via `transform:translate(0,-50%)` instead of centered) so 4 of them fit the vertical space without collision. Re-verified with a fresh screenshot — all four labels (Torg/Verkstad/Brygga/Strand) legible, non-overlapping, positioned along the left edge near the actual painted signpost.
  - Verified: `node --check` on the extracted script, the rewritten `test_trader.js` (drops all canvas/CTA/quick-fish assertions and the artificial walk-duration wait, since clicks are now synchronous; keeps every pure-function/economy/shared-boat/panel-content check), the full jsdom regression suite (`test2–6.js`, `test_dishes.js`, `test_dishking.js`, `test_boat_wizard.js`, `test_kaptensrutan.js`, `test_spota_ideas.js`) all green, and Playwright + real Chromium screenshots of the scene itself and of clicking through to the Verkstaden panel (confirms the panel still renders correctly below the image, nothing visually broken by the swap).
- **🏆 Flottiljen — fleet-wide leaderboard, and shared boat positions on the Seglandet 2026 map (added 2026-07-15):** requested as "add a separate leaderboard/status of all users and boats" plus "see or share location of boats on segling 2026 map." Both build on data that already existed client-side (`BOATS`/`sailors` were always loaded fleet-wide, not just for your own boat — `renderBoard()`'s existing "Andra båtar i flottiljen" mini-list proved this) but had never had a dedicated, full view.
  - **New `["fleet","🏆","Flottiljen"]` tab** in `NAV_MORE` → `renderFleet()` (in `#fleetBox`). Two sections: **"Flottiljens båtar"** — every boat in `BOATS` (not just your own), each showing its crew's combined points, current leg position (`tileFor()` against that boat's own `route`/`start_harbor`, falling back to `TILE_SHORT` for boats without a full 14-stop route), captain, and crew count. **"Alla seglare — topplistan"** — every sailor across every boat (not just `mySailors()`), globally ranked by `ptsFor()`, tagged with which boat they're on, reusing the exact `.lbRow`/medal/rank markup from the per-boat leaderboard in `renderBoard()` so it looks native; clicking a row opens `showProfile()` same as before. Refreshes `sailors`/`BOATS`/`logs`/`boatPositions` on tab open (`Promise.all([...]).then(renderFleet)`) since those aren't realtime-subscribed by default (only `logs`/`chat`/`settings`/`boats` and the new `boat_positions` are).
  - **Real bug caught and fixed before shipping:** the first pass wrapped the boat rows in a `.diagram` card (light "sand" background) using the bare `.row`/`.pts` classes — but those classes were only ever styled for the *dark body background* context (`.crewbarWrap .row`, and the existing "Andra båtar" list which renders straight onto the dark page, not inside a light card). Result: near-invisible light-on-light text, caught via a Playwright screenshot. Fixed by dropping the `.diagram` wrapper and rendering the rows directly on the page background, matching the established convention instead of inventing a new one.
  - **New `boat_positions` table** (migration applied live, `schema.sql` updated): `boat_id` (PK, one row per boat — a live "latest known position," not a track history), `lat`, `lng`, `sog`, `sailor_name`, `updated_at`. RLS open to `anon` (read/insert/update — same trust model as every other table), added to the `supabase_realtime` publication so position updates push live to everyone with the tab open, same pattern as `logs`/`chat`/`boats`.
  - **Sharing, on the Seglandet 2026 map:** a new **"📡 Dela position"** map button (`trkToggleShare()`), deliberately separate from the pre-existing "📍 Följ oss" (camera-follow only) and "⏺ Logga färd" (records a full per-sailor track for Törnrapport stats) — sharing is a third, lighter-weight, boat-level concern: while it's on, `trkPos()` throttle-upserts (every ~45s, same cadence as the existing `trkFlush`) a single row to `boat_positions` keyed by `myBoat().id`. Persisted across sessions the same way "Logga färd" already is (`localStorage.ss_trk_share`, resumed in `trkResumeIfWanted()`). `trkWatch()`'s on/off bookkeeping was extended to treat `TRK.share` as a third reason to keep the GPS watch alive, alongside `follow`/`rec`.
  - **Seeing others, on the same map:** a new **"🚢 Visa båtar"** toggle (`toggleFleetMarkers()`) fetches `boat_positions` and plots a marker per boat (`renderFleetMarkers()`, distinct green circle marker from the existing orange "this is you" `TRK.marker`, popup shows boat name + "X min sedan"); live-updated via the `boat_positions` realtime channel while the toggle is on. The Flottiljen tab also shows a plain-text summary of who's currently sharing, with a nudge to turn sharing on if nobody is — so the feature is discoverable without needing to already be on the map.
  - Both new map buttons and `loadBoatPositions()`/`saveBoatPosition()` are `LOCAL_MODE`-guarded (show a "Kräver server" toast and no-op) exactly like the existing `trkShowToday()`, since there's no shared backend to broadcast to offline.
  - Verified via a new `test_fleet.js`: two sailors on two different boats (one joining the pre-seeded boat, one creating a new one via the wizard) both appear in the fleet boat list and the global leaderboard, correctly ranked by points regardless of which boat they're on, with boat tags shown and profile-click wired up; plus the `LOCAL_MODE` guards for share/view/save all no-op safely with a toast instead of crashing. Full existing regression suite (`test2–6.js`, `test_dishes.js`, `test_dishking.js`, `test_boat_wizard.js`, `test_kaptensrutan.js`, `test_spota_ideas.js`, `test_trader.js`) re-run clean. **Note:** the live-map buttons themselves (Leaflet-dependent) couldn't be visually verified in this sandbox — same pre-existing `unpkg.com` CDN block documented in the 3D-village entry above, not something this change introduced; production already depends on that CDN successfully for the map that's been live for months.
- **🧳 Resor — trips-per-boat management page (added 2026-07-15):** requested as "a trips per boat management page, where can record previous harbours and logbook entries but plan also next trip." Two halves in one new `["trips","🧳","Resor"]` tab (`renderTrips()` → `#tripsBox`):
  - **Logbook of harbours already sailed:** the existing captain's-log feature (`renderCaptainLogCard()` — departure/arrival place+time, distance, weather chips, free-text notes, one entry per leg) was **relocated, not duplicated**, out of Törnrapport into Resor — it's a trip-management action, not a stat. `renderReport()` now shows a one-line pointer ("📓 Skepparens loggbok & resplanering flyttade till 🧳 Resor") instead of embedding the card. Caught and fixed a real bug in the same move: the card's own save-handler still called the old `renderReport()` to refresh itself post-save — harmless-looking but silently no-ops the correct tab's DOM when called from its new home; fixed to call `renderTrips()`.
  - **Planning the next trip (new):** new `trips` table (migration applied live, `schema.sql` updated) — `boat_id`, `name`, `status` ('planned'|'completed'), `stops` (jsonb array of `{place,date}`, ordered), `notes`. One row per named trip, several trips per boat (past *and* future — "resor" plural, not the fixed 14-leg `boats.route` used for the current voyage's scoring). Captain-only (`isCaptain()`, same gate as the logbook and route editor) can create a trip, add/remove/reorder stops (simple ↑/↓ swap buttons, harbor name via an `<input list>` datalist suggesting all known route/alt/extra harbor names), and toggle a trip planned ⇄ completed. Crew see the same cards read-only. RLS/realtime follow the same open-anon pattern as every other table.
  - **Real bug caught via testing, fixed before shipping:** `renderTrips()` is invoked both on tab navigation (fire-and-forget async) and again after every captain mutation (full refetch+re-render, for simplicity) — two overlapping in-flight calls could race and let a stale one clobber a fresher one. Fixed with a render-generation counter (`tripsRenderGen`) so only the most-recently-triggered call is allowed to touch the DOM; older in-flight calls quietly no-op.
  - **Test-writing pitfall worth remembering for this codebase:** broad `innerHTML.includes("Barösund")`-style substring assertions are unsafe here specifically because the new harbor-suggestion `<datalist>` (and, elsewhere, form placeholder text like `placeholder="t.ex. Höstresan 2026"`) puts real harbor names and example trip names into the DOM *unconditionally*, regardless of what data actually exists — a naive substring check can read "true" before the real save/render even happened. `test_trips.js` was written (and `test4.js` updated for the logbook's relocation) using precise markers instead (the formatted `"X → Y"` logbook line, numbered `"1. Hangö"` stop lines, the unique `"Återöppna resan"` button label) to avoid false positives.
  - Verified: `node --check`, new `test_trips.js` (create/add-stop/reorder/remove-stop/complete/reopen/persist-across-nav/delete, plus non-captain read-only), `test4.js` updated and re-passing for the relocated logbook, full regression suite (`test2–6.js`, `test_dishes.js`, `test_dishking.js`, `test_boat_wizard.js`, `test_kaptensrutan.js`, `test_spota_ideas.js`, `test_trader.js`, `test_fleet.js`) all green, and Playwright screenshots of the empty state and a trip with reorderable numbered stops.
- **🔭 Spana: +20 flora species and a new Fjärilar (butterflies) category (added 2026-07-15):** requested as "search and add additional 20 floras that are archipelago, also butterflies and backsippa." No code changes needed beyond the `GAMES` data object — `itemsFor()`/category chips/the custom-spot-add group dropdown/scoring are all already generically driven by `Object.entries(GAMES)`, so a brand-new category just works everywhere once added to the object.
  - **Flora & Land grew from 14 → 34 items**, appending 20 archipelago-typical plant species (Swedish name + verified Finnish name + short fact + rarity), sourced via web search against NatureGate/Finlands Artdatacenter/Suomen Lajitietokeskus rather than guessed, since a wrong Finnish name in a shipped bilingual nature-ID game is a real credibility risk. Highlight: **Backsippa** (`Kylmänkukka`, v5, rare) — explicitly requested by name — whose tip notes it "återfann sig i Finland just på Örö," which is a genuine botanical fact *and* one of the 14 real route harbors this boat's trip passes through. Also added **Strandkål** (v4, rare, genuinely uncommon) and **Strandaster** (v3, rare, salt-marsh specialist) as the other standout-rare entries; the rest (Havtorn, Fackelblomster, Gullviva, Prästkrage, Rölleka, Vresros, Strandråg, Örnbräken, Strandkvanne, etc.) are common v1–v2 sightings, matching the existing category's rarity distribution.
  - **New `butterflies:{label:"Fjärilar",...}` category, 10 species.** Common ones (Amiral, Citronfjäril, Påfågelöga, Nässelfjäril, Kålfjäril, Blåvinge, Tistelfjäril) at v1–v2, plus three genuine rarities at v3/v5: Silverstreckad pärlemorfjäril, Makaonfjäril (Finland's largest day butterfly), and **Apollofjäril** (`Isoapollo`, v5) — a real protected/endangered species whose Finnish range is specifically the southwestern archipelago this game is set in, so its tip ("finns bara i sydvästra skärgården") is literally true for this crew, not generic flavor text.
  - Verified via a new `test_flora_butterflies.js`: the Fjärilar chip appears alongside the existing categories, Flora & Land now reports 34 items, Backsippa renders with its Finnish name and rare styling, Apollofjäril is flagged rare and logging it awards the correct 5 points, and the new category is selectable in the "➕ Lägg till art" custom-spot group dropdown — all without touching any rendering code, confirming the category system really is fully data-driven. Full existing regression suite re-run clean, plus Playwright screenshots of both category grids at a 390px mobile viewport (rare cards show the expected gold/orange highlight, no layout breakage, diacritics render correctly).
- **🍳 Points for cooking (added 2026-07-15):** requested as "poäng också den som lagar mat" (points also for whoever cooks). Built as a straight sibling of the existing dishwashing bonus (`DISH_PTS`) rather than a new mechanism — `COOK_PTS=3` (a notch above dishwashing's 2, since cooking a meal is more effort than doing the dishes, in line with `ROLE_PTS=3`), a "Byssan" card in 🎁 Dagens (`renderSur()`) with a `cook:<date>` bonus ref, anyone aboard can claim it once per day (same `hasLog`/`addLog("bonus",...)` pattern, same `one_per_sailor` DB constraint already covering all `bonus` logs — no schema change needed), and a **"🍳👑 Kockmästaren"** champion card in Törnrapport mirroring "🍽️👑 Diskmästaren" exactly, plus a "🍳 Lagat mat" row in each sailor's per-person breakdown. Cooking and dishwashing stack independently the same day since they're different `ref` prefixes.
  - Verified via a new `test_cooking.js` (card renders alongside the existing dish card without replacing it, points award correctly, done-state hides the button, cooking + dishwashing points stack independently, Kockmästaren champion card and per-sailor row appear in Törnrapport) plus the full existing regression suite re-run clean.
- **Ratings & reviews extended to the reserve harbours (added 2026-07-15):** requested as "add rankings and reviews to the other harbours also" — the 14 main route `PLACES` already had a star-rating seed, crew reviews (`port_notes`, photo upload included), and a "✍️ Loggboka hamnen" button; the 9 `ALT_HARBORS` + 15 `EXTRA_HARBORS` ("Fler hamnar i området") only had a static blurb, no way to review them at all.
  - **Unified the two separate card renderers into one** (`buildHarborCard(p,numLabel)`, replacing the inline `PLACES.forEach` block and the standalone `harborCard` arrow function) so every harbor list in `renderPlaces()` gets identical treatment — existing reviews, a live star average, and the "Loggboka hamnen" button/form — with only the route-order number badge (`numLabel`) staying exclusive to the 14 main stops, since alt/reserve harbours aren't part of the fixed route.
  - **No schema change needed:** `port_notes.place` was always a plain text match on harbor name, not tied to `PLACES`' array index, so the existing table already worked for any harbor name — it just needed a UI entry point on the other two lists.
  - `noteFormOpen` (which form is currently expanded) changed from a `PLACES`-array index to the harbor's name string, so the same toggle state works across all three lists without key collisions.
  - **New `harborRatingLine()` helper:** alt/extra harbours have no pre-seeded `r` rating (unlike `PLACES`), so it computes a live average + review count from crew submissions once any exist (`★ 4.5 (2)`), falling back to nothing until the first review — while `PLACES` entries keep showing their static seed rating exactly as before, unaffected.
  - **New `harborMapsHref()` helper** preserves a real edge case from the old code: a few `ALT_HARBORS`/`EXTRA_HARBORS` entries (e.g. Kirjais, Villa Wolax) have no `lat`/`lng`, so the map link falls back to a name-based Google Maps search instead of coordinates — same behavior as before, just centralized.
  - Verified via a new `test_harbor_reviews.js` (a reserve harbour now renders the review button and openable form exactly like a main-route one, the lat/lng-less fallback map link still works, `EXTRA_HARBORS` entries get the same treatment once "Fler gästhamnar" is expanded, and the rating-line helper's seed-vs-live-average-vs-empty logic is unit-tested directly) plus the full existing regression suite re-run clean, plus a Playwright screenshot confirming reserve harbour cards now show the same "✍️ Loggboka hamnen" button as the main route.
- **🧳 Resor trip planner: distance, ETA and wind per stop (added 2026-07-15):** requested as "in planning show planned winds and distance + estimated time." Reused the exact same building blocks as the pre-existing Kaptensrutan → 🧭 Planeringsläge feature (`harborCoords()`, `haversine()`, Open-Meteo) rather than inventing a second system.
  - **Distance + ETA, leg by leg:** new `harborDistNm(nameA,nameB)` (haversine between two `harborCoords()` lookups, in nm) computes each stop's distance from the *previous* stop in the itinerary — or from the boat's `start_harbor` for the first stop — and divides by a per-trip "Antagen fart" (assumed speed, default 5 kn, a plain number input, not persisted — ephemeral UI state in a new `tripSpeeds` map keyed by trip id, re-rendered live on input same as Planeringsläge's own speed field). Unknown/mistyped harbor names degrade to "Okänt avstånd" instead of crashing, since stop names are free text (guided by the `#harborSuggest` datalist, but not enforced).
  - **Wind, but date-gated:** the existing `windForecastAt()` used by Planeringsläge only pulls a 12-hour-ahead forecast (fine for "the next stop, live"), which isn't useful for a trip planned weeks out. New `windForecastForDate(lat,lon,dateStr)` requests a 16-day Open-Meteo forecast and picks the hourly slot closest to noon on the stop's planned date, returning `null` (not throwing) if the closest match is >36h off — i.e. the date is too far in the future for a forecast, or already in the past. Each stop shows one of three states: "Lägg till datum för vindprognos" (no date set yet), "Hämtar vindprognos…" → resolves to either the wind line or "Ingen vindprognos ännu (för långt fram eller redan passerat)". Wind fetches are fired per-stop, async, patching each stop's own `#tripWind-<tripId>-<index>` span in place once resolved — doesn't block the rest of the card from rendering.
  - **Sandbox verification note:** `api.open-meteo.com` is blocked by this coding sandbox's outbound proxy (confirmed via `curl`, same 403 pattern as the earlier-documented `jsdelivr`/`unpkg` CDN blocks) — so the actual forecast response couldn't be fetched live here. What *is* fully verified: the offline-safe fallback path (fetch throws → caught → resolves to `null` → UI shows the graceful "no forecast" message, never a crash), since `fetch` is also undefined in the jsdom test harness, exercising the exact same code path. Production already depends on this same Open-Meteo endpoint successfully for the existing top-of-app wind bar and Planeringsläge, so there's strong precedent it'll resolve real data for actual users.
  - Verified via a new `test_trip_wind_eta.js`: `harborDistNm`/`windForecastForDate` unit-tested directly (sane distance for two known harbors, `null` for an unknown one, offline-safe resolution), a first stop's distance is measured from the boat's start harbor, a second stop's distance is measured from the first stop (not the boat start), the date-gated wind states show correctly, and raising the assumed-speed input live-recomputes the ETA (roughly halves when speed doubles). Full existing regression suite re-run clean, plus a Playwright screenshot confirming the layout reads well on a 390px mobile viewport.
- **☕ Points for coffee-making + 🐛 Insekter category (added 2026-07-15):** requested as "points for coffee making and to archipelago insects also seen in area."
  - **Kaffe kokat:** third sibling of the cooking/dishwashing bonus pattern — `COFFEE_PTS=2` (same weight as dishwashing), a "Kabyssen" card in 🎁 Dagens sitting between the existing "Byssan" (cooking) and "Husgeråd" (dishes) cards, `coffee:<date>` bonus ref, same once-per-sailor-per-day claim rules. New **"☕👑 Kaffemästaren"** champion card in Törnrapport alongside Kockmästaren/Diskmästaren, plus a "☕ Kaffe kokat" row per sailor. Coffee, cooking, and dishwashing all stack independently the same day.
  - **New `insects:{label:"Insekter",...}` Spana category, 10 species** — archipelago-typical insects distinct from the existing Fjärilar (butterflies) category, names verified against Finlands Artdatacenter/laji.fi rather than guessed (same discipline as the earlier flora/butterfly batch). Common sightings at v1–v2 (Fyrfläckad trollslända, Humla, Honungsbi, Nyckelpiga, Stackmyra, Geting, Gräshoppa, Sjöflickslända, and — deliberately included for a laugh, since it's genuinely the most reliable sighting of the trip — Mygga), plus one real standout: **Kejsartrollslända** (`Keisarikorento`, v3, rare) — the Nordic region's largest dragonfly. As with the earlier butterflies category, no rendering code changed; `GAMES` categories are fully generic over `Object.entries()`.
  - Verified via a new `test_coffee_insects.js`: the coffee card renders alongside cooking/dishes without replacing them, points award correctly and stack independently, the Kaffemästaren champion card and per-sailor row appear in Törnrapport, the Insekter chip/category renders with all 10 species and correct rarity styling, logging an insect awards the right points, and the category is selectable in the custom-spot-add dropdown. Full existing regression suite (18 test files total) re-run clean, plus Playwright screenshots of both new UI surfaces.
- **UX audit follow-through: grouped "Mer" menu + tap-to-arm delete confirmation (added 2026-07-15):** requested after a UX-friendliness review flagged (1) the "Mer" sheet as a 15-item flat unsorted list and (2) every destructive action in the app (delete trip, remove stop, delete logbook entry) firing instantly on tap with no confirmation and small adjacent tap targets.
  - **Grouped "Mer" sheet:** new `NAV_MORE_GROUPS` (display-only grouping — `NAV_MORE` itself, the single source of truth for keys/icons/labels, is untouched) buckets the 15 items into 4 labeled sections — Besättningen, Planering & navigation, Lära & leka, Läten & musik — rendered under new `.moreGroupLabel` headers in `buildTabs()`. Purely a rendering change; navigation/keys unaffected.
  - **Tap-to-arm, tap-again-to-confirm on all destructive buttons**, replacing native `confirm()` (out of place in this app's fully custom UI) — new shared `wireDelBtn()`/`delBtnHtml()`/`isArmed()` helpers applied to trip delete, trip-stop delete, and captain-log-entry delete. First tap turns the button into a bold red "Säker?" pill (bigger, unambiguous tap target — also fixes the separately-flagged tiny-adjacent-icon-buttons issue, now `.delBtn`/`.mvBtn` both get a real ≥34px hit area); a second tap within 3 seconds confirms, otherwise it auto-reverts. Arming is a **purely local DOM mutation** on the button itself (no re-render, no network) — deliberately, after an earlier version that re-rendered the whole section on arm was found to flash a "Hämtar resor…" loading state on every single tap.
  - **Two real bugs found and fixed via testing, not just added scaffolding:** (1) an early version of the captain-log delete handler used a closure-captured `card` element to patch the DOM locally on delete; across `armDelete`'s two re-renders (immediate arm + delayed 3s auto-expire-if-unconfirmed) that closure went stale, leaving orphaned duplicate-`id` nodes in the DOM that broke *scoped* `querySelector('#id')` lookups (a genuine, confirmed DOM/jsdom gotcha: a scoped ID-selector query can return `null` when the same id exists elsewhere in the document, even outside the query's own subtree) — fixed by routing the confirm step through the same stable top-level `renderTrips()` full-reload every other trip mutation already uses, instead of a bespoke local patch. (2) `pendingDeletes` was originally a plain `Set` keyed by list-position strings like `"stop:<tripId>:<index>"` — since indices get **reused** once an item is deleted and a new one lands in the same slot, a stale auto-expire timer from the *old* item at that index could silently clear the *new* item's armed entry (same key) without touching its now-different button element, permanently desyncing the visible "armed" state from the tracked one. Fixed by making `pendingDeletes` a `Map` of key→token; each arm event gets a fresh token, and a timer only acts if its token is still the current one for that key (same staleness-guard pattern as `tripsRenderGen` used elsewhere in Resor).
  - Verified via a new `test_ux_improvements.js` (all 4 group headers render with all 15 items still present and navigable; arm/confirm/auto-expire all behave correctly on trip and stop deletion) plus a dedicated stress-test scenario (arm a delete, navigate to a completely different tab before the 3s auto-expire fires, confirm no crash and no orphaned DOM nodes) that caught bug (1) above. `test4.js` and `test_trips.js` updated for the new two-tap delete interaction (they pre-dated this change). Full 19-file regression suite re-run clean, plus Playwright screenshots of the grouped sheet and the armed "Säker?" confirm state.
- **Maskinchef gets real engine/oil knowledge + visual "high end" overhaul (added 2026-07-16):** requested as three lines in one message — "Overhaul the basic buttons and graphics so they look high end", "Should Machine engineer know what oil is in or what the engine is like?", "Yes a more advanced menu and button structure."
  - **Engine + oil boat specs:** extended the existing generic `BOAT_SPEC_FIELDS` system (already auto-wiring Model/LOA/Beam/Draft/Mast/Weight/Rig into the new-boat wizard, the Kaptensrutan boat-edit form, and Frågesport quiz generation with zero rendering-code changes) with two new entries — `Engine`/`Motor` (`BOAT_ENGINE_POOL`: Volvo Penta/Yanmar/Vetus/Perkins models typical of Baltic archipelago cruisers) and `Oil`/`Motorolja` (`BOAT_OIL_POOL`: realistic diesel motor-oil grades). Both auto-appeared in the wizard/Kaptensrutan forms and gained an auto-generated quiz question via `boatSpecQuestions()` for free, exactly as designed. Required a real schema change (unlike every earlier `BOAT_SPEC_FIELDS` addition, which reused existing generic columns) — `boats.engine`/`boats.oil` text columns added via a live migration and mirrored into `schema.sql`.
  - **Maskinchef's daily task pool is now boat-aware:** new `maskinchefTasks()` takes the static 3-task pool and, when the boat has `engine`/`oil` set, appends two more tasks referencing the *actual* configured values (e.g. "Kontrollera Volvo Penta MD2040 — lyssna efter ojämn gång…", "Kolla oljenivån — Vindpust ska ha 15W-40 dieselolja."). `roleTaskFor()` now special-cases `"maskinchef"` to pull from this dynamic pool instead of the static `ROLE_TASKS` one; every other role is untouched. Falls back to the original generic tasks if the boat has no engine/oil set (e.g. existing boats pre-dating this feature). Also added to both read-only "Om båten" summary lines (Kaptensrutan card, `myBoatSpecs()`-derived spec line) — the hull-diagram caption in Sjötermer deliberately excluded, since that one lists only physical dimensions (LOA/beam/draft/mast/weight), not machinery.
  - **Visual "high end" pass — CSS-only, touching only the shared cascading classes** identified earlier this session as the highest-leverage spot (glossy buttons `.goBtn/.liveBtn/.cpBtn/.qnext/.play/.logBtn`, shared card border+shadow group, bottom tab bar, "Mer" sheet, HUD chips/coin, weekbar/scorebar/toast) rather than touching any per-feature call site:
    - **Buttons:** added a diagonal glass-sheen highlight (`::after` pseudo-element, `overflow:hidden` on the button itself) and a second, blurred ambient-glow shadow layer alongside the existing hard "pressed" offset shadow — keeps the game's tactile chunky-3D press interaction exactly as-is, just adds real depth and shine on top of it.
    - **Cards** (`.card,.scard,.pcard,.qcard,.diagram,.cpCard,.surCard,...`): thinner, softer border; added a blurred ambient drop-shadow layer plus an `inset` top-edge highlight for a glass/lacquered feel — deliberately done via extra `box-shadow` layers only (never touching `background`), since an earlier draft using a `background-image` overlay would have silently nuked every card's `background:var(--sand)` due to CSS's shorthand-property-reset behavior on an equal-specificity, later-declared rule (caught before shipping, not after).
    - **Bottom tab bar + "Mer" sheet:** `backdrop-filter: blur()` glass panels (graceful no-op on unsupported browsers), deeper layered shadows, active-tab now gets a soft color-matched glow in addition to its existing lift/hop animation.
    - **"Mer" menu, structurally upgraded:** each of the 4 groups added in the prior UX-audit pass (Besättningen / Planering & navigation / Lära & leka / Läten & musik) now renders its rows with a distinctly color-tinted rounded icon badge (`.moreRow.g0`–`.g3`, orange/blue/green/gold matching the app's existing accent palette) instead of a bare emoji — answers "more advanced menu structure" concretely: groups are now visually scannable by color, not just by header text. Implemented via one added `gi` group-index in `buildTabs()`'s existing loop, zero new DOM structure.
    - **HUD chips, coin badges, weekbar/scorebar/toast:** matching blur+layered-shadow polish for visual consistency across every "floating panel" surface in the app.
  - Verified via a new `test_boat_engine_oil.js` (wizard renders the new fields, `myBoatSpecs()` exposes them, `maskinchefTasks()`'s pool grows by exactly 2 boat-specific entries without dropping the 3 generic ones, Kaptensrutan edit form includes both fields) and an extended `test_ux_improvements.js` (every "Mer" row carries its group's accent class). Full 19-file regression suite re-run clean. Playwright screenshots at a 390px viewport across Plan/Spana/Dagens/Mer confirm the glass tab bar, glowing active tab, color-coded Mer-menu icon badges, and sheen-highlighted primary buttons all render correctly with no layout breakage.
- **More Frågesport questions + pop-quiz now fires several times a day and can cost points (added 2026-07-16):** requested as "Where is the idélådan? Also make sure captain can manage, order, add and delete stops. Add questions to frågesport and add more opted pop quizzes that can give minus points also" (a typo in the live message was corrected mid-conversation: "opted" → "more often").
  - **Idélåda location (no code change, just an answer):** it's already in the "Mer" menu, "Besättningen" group (💡 Idélåda) — `NAV_MORE`/`NAV_MORE_GROUPS`, `ideasView`/`ideasBox`.
  - **Captain stop management (no code change, verified already complete):** `renderTripCard()` already gates add/reorder(↑↓)/delete of trip stops behind `isCaptain()` (`addTripStop`/`moveTripStop`/`removeTripStop`, wired at the `[data-tsadd]`/`[data-stopmv]`/`[data-stopdel]` handlers) — confirmed still passing in `test_trips.js`/`test_ux_improvements.js`, no changes needed.
  - **Frågesport (`QUIZ`) grew by 8 questions** — COLREG right-of-way, IALA region, nautical-mile definition, Finnish life-jacket law, sailing terminology, VHF, recognized distress signals — same verified-fact discipline as every earlier content batch this session, no rendering changes needed since `allQuiz()` is just `QUIZ.concat(boatSpecQuestions(...))`.
  - **Pop-quiz overhauled from "once a day, wrong = 0" to "several times a day, wrong costs points":** `POPQUIZ` grew by 6 regular questions plus 6 harder "SVÅR:" ones carrying explicit `v`/`pen` overrides (worth +4 to +6 for a correct answer, −2 to −3 for a wrong one, vs. the regular +2/−1 default) — same array, same `allPopQuiz()`, no new data structure. Rewrote `maybePopQuiz(force)`: refs changed from a single `pop:<date>` to `pop:<date>:<n>` (`n` = today's count so far), so up to `POPQUIZ_MAX_PER_DAY=5` independent attempts can be logged per sailor per day instead of one. `enterApp()`'s existing post-login trigger now calls `maybePopQuiz(true)` (forced — bypasses the chance/cooldown gate, preserving the "always see one when you open the app" guarantee), and `navTo()` gained a second, *non-forced* call on every tab switch — gated by a 4-minute cooldown (`POPQUIZ_COOLDOWN_MS`) plus a 35% roll — so quizzes resurface through a session without popping on literally every navigation. Wrong answers now `addLog("quiz",ref,-pts,...)` instead of `0`, so a bad pop-quiz answer genuinely lowers the sailor's total (schema already allowed negative `points`, no migration needed; `rankFor()` already floors gracefully at "Jungman" for negative totals — checked, not assumed).
  - Verified via a new `test_popquiz_minus.js` (deck size grew; a forced call always opens regardless of cooldown; the shown point value matches each question's own `v`/`pen`; two same-day attempts log under distinct `pop:<date>:0`/`pop:<date>:1` refs; the `POPQUIZ_MAX_PER_DAY` cap is enforced even for forced calls; repeated `navTo()` calls don't crash or stack popups) plus the full 21-file regression suite re-run clean. Playwright screenshots confirm the pop-quiz modal shows the correct "Rätt svar = +4 p · fel svar = -2 p" style copy per question, the wrong/right color states render, and the toast reflects the actual (possibly negative) point delta.
- **Bug fix: "Mer" sheet couldn't scroll and cut off the bottom groups (fixed 2026-07-16):** reported as "Mer menu has gotten long and cannot scroll it I think." Real regression from the earlier grouped-Mer-menu work: `.moreSheet` was `position:fixed` with a `bottom` offset but no `top`/`max-height`, so it simply grew with content — with 4 group headers + 15 rows it could exceed the viewport height, and having no `overflow-y` meant the overflowed rows (confirmed: "Lära & leka" and "Läten & musik" on a 700px-tall viewport) were completely unreachable, not just visually cut off. Fixed with `max-height:min(60vh,calc(100vh - 140px));overflow-y:auto;-webkit-overflow-scrolling:touch;overscroll-behavior:contain;` — caps the sheet well within any viewport and makes it scroll internally.
  - Verified via Playwright at a deliberately short 390×700 viewport (small enough to force the overflow that a taller device might not hit): `moreSheet.scrollHeight` (965px) exceeds `clientHeight` (418px) confirming real overflow exists, `overflow-y` computes to `auto`, and after scrolling the sheet's own scrollTop to the bottom the last row ("Sjömansvisor") lands fully inside the viewport. Extended `test_ux_improvements.js` with computed-style assertions (`max-height !== "none"`, `overflow-y === "auto"`) so this can't silently regress again. Full 21-file regression suite re-run clean (`test_fleet.js` failed once under parallel load, passed cleanly in isolation — the same pre-existing flakiness documented earlier this session, unrelated to this change).
- **📊 Törnrapport: "Dagens överblick" with surprising aggregate stats (added 2026-07-16):** requested as "getting a day overview that reads logbook entries and points and game successes and shows surprising metrics like women more knowledgable in vessels, activity highest before 12, or children under 10 smashed insects and flora today." Two-part build: new optional sailor demographics + a new report card that mines existing log data for real (guarded, not fabricated) daily patterns.
  - **New optional, self-reported `sailors.gender`/`sailors.age_group` columns** (migration applied live + `schema.sql` updated; `CHECK` constraints restrict values to `F`/`M`/`A` and `barn`/`ungdom`/`vuxen`) — the only new data this feature needed, since category (`ref.split(":")[0]` on `spot` logs) and time-of-day (`created_at`, already on every log row) were already fully derivable from existing data with zero schema changes.
  - **Collected once, editable anytime, never shown per-person:** a new `demographicsFieldsHtml()`/`demographicsValues()` pair (mirroring the existing `specFieldHtml`/`specFieldValue` pattern for boat specs) renders two optional selects — defaulting to "Vill inte säga" — wired into both the sign-up form (`newSailorForm`) and, for editing later (existing crew had no way to backfill this), a new section in `showProfile()` that only renders when viewing your *own* profile, saved via a new `saveMyDemographics()`. `loadSailors()`'s explicit column `select(...)` list had to be extended to include the two new columns (it wasn't `select("*")`) — an easy miss that would've silently discarded the data from every remote sync.
  - **Real bug found via screenshot review, not caught by the first pass of tests:** `showProfile(who)` can receive the lightweight session object (`me`, which only carries `id/name/emoji/boat_id`) rather than the full sailors-array record, so the demographics selects were always pre-filling blank even for a sailor who'd already set them at sign-up. Fixed by resolving `sailors.find(x=>x.id===who.id)` inside `showProfile` before reading `gender`/`age_group`. Worth noting *how* this was caught: the automated test only verified that saving an edit updated the record (which worked regardless), not that the form loaded with the right starting values — a Playwright screenshot of the profile view was what actually exposed the bug. Test extended afterward to assert the pre-filled values match what sign-up saved, so this can't silently regress again.
  - **`renderDayOverview()`**, a new `repCard` rendered first in Törnrapport (ahead of "Så seglade ni"), built from `todaysLogs()` (a straight `allLogs()` filter on `created_at`'s date matching `today()`): total points/log-count/active-sailors logged today, a busiest-half-of-day comparison (before/after 12:00, only shown once ≥4 logs exist today so a 1-log day can't produce a spurious "activity was highest" claim), the day's top Spana category by log count (only shown at ≥2 sightings), and — the part mirroring the user's specific examples — a **single** most-lopsided group-vs-group comparison per demographic axis (gender, age bracket), computed by `categoryGroupLeader()`.
  - **`categoryGroupLeader()` is deliberately conservative**, because this runs on real friends'-and-family's data, not a toy demo: for a category+group comparison to be surfaced at all, it requires *at least two distinct groups* to have logged in that same category today AND *at least 3 combined sightings*, and it only ever reports the single largest gap found (never a noisy list of every category). A 1-vs-0 split (one sailor logged something nobody else touched) is explicitly not treated as a finding. Output is always an aggregate count comparison ("Barnen (under 10 år) loggade flest inom Insekter idag · 2 st") — never a per-person statement about a specific child or adult, matching the "visas aldrig per person" promise shown right where the data is collected.
  - **"Dagens bedrifter" (today's game successes):** reuses the existing cook/coffee/dish/surprise bonus refs (`cook:<date>` etc., already date-scoped) filtered to *today specifically* via `todaysBonusHighlights()`, listing which sailors claimed which daily bonus today — the "reads... game successes" part of the request, using data that already existed.
  - **Real test-authoring bug caught and fixed during this work (documented since it's a reusable lesson for this codebase):** the first version of the new test waited for the literal text "Dagens överblick" to appear in `#reportList` after navigating — but that div is only hidden via `display:none` between tab switches, not cleared, so leftover text from an *earlier* completed render could satisfy the wait instantly, before the fresh render (triggered by the just-fired `navTo`, itself delayed ~230ms by the wave-sweep transition) had even started — producing a test that read genuinely stale DOM content and reported wrong pass/fail results despite the underlying app logic being correct the whole time. Fixed by waiting for content that can *only* exist once the specific expected fresh state has rendered (e.g. a specific sailor's name, or an exact log-count value) instead of a generic heading string — consistent with the "avoid broad substring assertions" lesson from earlier in this session, now extended to "and don't trust presence-only waits against panels that persist hidden (not cleared) across tab switches."
  - Verified via a new `test_day_overview.js` (sign-up fields present and default to non-committal; values persist to the sailor record; profile-edit pre-fills and updates correctly; the day-overview card renders safely with zero data; a lone sailor's lone spot log produces no gender/age claim per the sample-size guard; adding a second, differently-tagged sailor's comparable activity in the *same* category correctly produces one aggregate, never-per-person claim) plus the full 22-file regression suite re-run clean. Playwright screenshots confirm the sign-up form, the rendered day-overview card, and the profile edit section all look right on a 390px mobile viewport — screenshot review is what caught the pre-fill bug above, underscoring why it's part of this workflow and not just the automated assertions.
- **🎣🏃 Two new mini-games inside Fiske & Handel: a live hook-fishing game and a "Till Torget" runner (added 2026-07-16):** requested via screenshots of a classic Mario-style platformer and three different mobile fishing-game styles ("Hooked"-style falling/reeling with combos, a decorated fish-tank game, a simple drag-hook catcher), with the ask "when going to city [use] a super Mario / go go Mario like [game] and when going boating the various fishing games." Scoped with the user via `AskUserQuestion` first, since this was a much bigger build than prior single-feature asks: (1) the new fishing mechanic **replaces** the old cast/wait/tap-timing-bar entirely rather than sitting alongside it, (2) the platformer is **triggered from Torg** (the market signpost) rather than being a new top-level Arcade entry, (3) both aim for **richer custom graphics**, not the plain emoji/canvas-primitive style of Tetris/Snake.
  - **"Richer graphics" without new asset-generation risk:** discovered the repo already ships two illustrated art assets (`fiske-village.jpg` — a painted archipelago-village scene with Torg/Verkstad/Brygga/Strand, already used as the Trader map's background; `boat.jpg` — a watercolor sailboat, used on the splash screen) rather than generating brand-new art via an image model mid-feature. Both new game canvases (`#fishCv`, `#runCv`) are transparent-background (`ctx.clearRect`, never an opaque `fillRect` for the base scene) with CSS `background:url('fiske-village.jpg') <pos>/cover` behind them, so the existing painted dock/water scene (fishing) and village-path scene (runner) show through as a real illustrated backdrop, with only the *dynamic* game elements (fish, hook, hazards, character, obstacles, coins, particles) drawn per-frame on top. Meaningfully upgrades visual richness for zero new asset risk.
  - **Krokfiske (hook fishing), replacing the old mechanic:** `renderFishUI()`'s active-session branch now renders a canvas instead of a button/timing-bar; a new `FISHG` state + `startFishGameLoop()`/`fishGameStep()`/`fishGameDraw()` drive a continuous drag-the-hook game — the hook eases toward wherever the player drags/taps (pointer + touch events), fish of the existing `FISH_SPECIES`/`BEACH_FISH_SPECIES` pools swim across at varying depths/speeds via the **same** `rollSpecies(pool, rodLevel)` weighted/rod-gated roll used before, and new floating hazards (🛞🪼🌿) briefly "snag" the hook (900ms cooldown) if caught carelessly. Every economy hook from the old mechanic is untouched: `addCatch`, the once-daily rare-species "storfångst" bonus (`FISH_PTS`, `fish:<date>` ref), session length + fuel/sails bonus duration, `ws.rodLevel` — now repurposed as a bigger hook hit-radius (`fishHookRadius()`) instead of a wider timing-bar zone. The countdown-driven 250ms re-render (needed to keep the "Xs kvar" text current) was changed to detect an already-running canvas and only patch the text nodes, instead of tearing down and restarting the whole canvas/animation loop 4x/second — a real bug that would have reset the game silently on every tick if left as the original per-render rebuild.
  - **Till Torget, a new runner gating the market:** tapping "Torg" now shows an intro card — "🏃 Spring dit! (samla mynt)" or "➡️ Gå direkt till marknaden" — before reaching the existing sell screen (renamed internally to `renderMarknadSell`, unchanged otherwise). Choosing to run starts a fixed-length (not endless) side-scrolling jump-over-obstacles-collect-coins course (`startRunGameLoop()`/`runGameStep()`/`runGameDraw()`), deliberately **not** recreating any specific platformer's iconic character/block/enemy designs — it's the generic "jump one button, dodge things, grab coins" genre (the same genre as, e.g., the Chrome dinosaur game), reskinned as a sailor jogging up the archipelago path to the village market. **No fail state on purpose:** getting hit costs a brief invulnerability flash, never the run itself, since this is flavor/bonus content layered onto the existing fishing/trading economy, not a skill gate — every run ends by reaching the target distance, then converts collected coins into `ws.kronor` (shared boat economy) plus a capped once-daily `minigame` point log, matching the `arcAwardBtn` pattern used by every other Arcade game.
  - **Real bugs found and fixed via rigorous testing, not glossed over:** (1) `ctx.getContext("2d")` returns `null` under this project's jsdom test harness (no `canvas` npm package installed, confirmed no existing test had ever exercised Tetris/Snake's canvas draw calls either — this was the *first* test-exercised canvas code in the suite) — both new draw functions now guard `if(!ctx)return;`, genuine defensive coding, not just a test shim. (2) `requestAnimationFrame` is `undefined` entirely under the same harness (confirmed via a minimal repro) — both `startFishGameLoop()`/`startRunGameLoop()` now check `typeof requestAnimationFrame!=="function"` and skip starting the animation loop if absent, while still wiring up all state/DOM/event-handlers so the surrounding UI and pointer/keyboard bindings remain fully testable. (3) Rewrote `test_trader.js`'s fishing-flow and Marknad-flow sections to drive the underlying pure functions directly (`fishCaught()`, `hazardHit()`, `runGameStep()`, `runFinish()`) instead of simulating pointer drags (which can't work meaningfully against jsdom's zeroed `getBoundingClientRect()`), mirroring how Tetris/Snake's internal logic is already tested elsewhere in this suite rather than via full pointer simulation.
  - **Known sandbox-only rendering artifact, investigated and confirmed environmental (not a code bug):** in this coding sandbox's headless, software-rendered Chromium, canvas `fillText()` of complex color emoji (🏃, fish species, hazards) renders with very low/no visible contrast in screenshots — confirmed via direct pixel sampling (`getImageData`) that the glyphs genuinely are being drawn (100+ non-transparent, correctly-colored pixels at the exact expected coordinates) even though they're hard to see in a compressed screenshot, and confirmed this is **pre-existing**, not introduced by this change — the already-shipping Snake game's 🐟 food emoji shows the identical faintness when screenshotted the same way. `NotoColorEmoji.ttf` is installed in the sandbox, so it's specifically a Canvas2D color-glyph compositing quirk under this headless/software-rendering setup, not a missing font. Real phones/desktop browsers render color emoji via canvas `fillText` reliably (an extremely common, well-supported technique) — added semi-transparent white "backing discs" behind every sprite regardless, both as a real contrast improvement against the busy illustrated backdrop on any device and to make the sandbox screenshots at least show *something* at each sprite's position for verification purposes.
  - Verified via a substantially rewritten `test_trader.js` (fishing: canvas wiring, mid-session re-render doesn't restart the loop, `fishCaught`/`hazardHit` economy integration, rare-species bonus; Torg: intro-then-skip reaches the sell screen, intro-then-run shows the runner canvas, `runGameStep` runs repeatedly without crashing, `runFinish` shows the summary and credits kronor, continuing reaches the sell screen, leaving and re-entering Torg resets to the intro rather than a stale screen) plus the full 22-file regression suite re-run clean. Verified in a real (non-headless-limited) Chromium via Playwright: the village map, fishing canvas (illustrated dock/water backdrop, draggable hook line, HUD), Till Torget intro card, and runner canvas (coins, obstacles, ground planks, progress %, HUD) all render and respond to drag/keyboard input with zero page errors.
- **Till Torget runner reworked into an authentic side-scrolling platformer (added 2026-07-16, follow-up to the mini-games above):** requested as "make the running game that is like a super Mario mini game more authentic." The first cut was a bare one-lane runner (emoji obstacles 🦀🪨📦 on a brown strip). Rebuilt `RUNG`/`startRunGameLoop`/`runGameStep`/`runGameDraw` into a genuine platformer with the genre's *defining* mechanics — the point was to add the interactions that make a Mario level feel like Mario, not just reskin obstacles:
    - **Bump ❓ blocks from below** (jump up so the sailor's head hits a floating block → it turns brown/used and pops a coin, or ~35% of the time a mushroom); **stomp enemies from above** (fall onto a goomba-ish critter → squash it, bounce, +coin — or take a hit if you run into its side instead); **hop over green pipes**; **grab the mushroom power-up** (+3 coins and a brief "big" sailor); **finish on a flagpole** (a nautical pennant on a pole scrolls in at the end and triggers a short slide-down before the summary). The stomp/head-bonk collision uses a shared height-above-ground coordinate (`feetH`/`headH`) with vy-direction checks so "landing on top" vs "running into the side" resolve distinctly, the same way a real platformer distinguishes them.
    - **Original artwork, not Nintendo's:** the "more authentic" ask is satisfied through genre conventions + original canvas-drawn art, deliberately NOT trademarked sprites — the hero is a **sailor** (navy/white striped shirt + sailor cap with running-leg animation), the enemy is a generic brown goomba-shaped critter, the flag is an orange nautical pennant. No red-cap/blue-overalls plumber, no copied block/enemy designs. Kept the established "no fail state" philosophy (a hit is a brief invulnerability flash, never game-over — this is bonus economy content).
    - **Everything drawn with canvas primitives** (sky gradient, Mario-style overlapping-circle clouds, two parallax hill layers, grass-topped dirt ground with scrolling seams, gold ? blocks, green pipes with lips, spinning gold coins, the sailor, the goomba, the flagpole) rather than emoji `fillText` — this both looks crisper/more authentic AND sidesteps the documented sandbox canvas-emoji-contrast quirk entirely, so the game now renders identically well in the headless sandbox and on real devices. The `#runCv` CSS background was switched from the watercolor village image to a plain sky-blue fallback since the canvas now paints its own complete opaque platformer scene (the fishing canvas still uses the illustrated water backdrop — that one benefits from it). Particle popups (`+1`, `+3 🍄`, `💥`) now render with a dark outline for legibility over the bright scene.
    - **Everything drawn with canvas primitives** (sky gradient, Mario-style overlapping-circle clouds, two parallax hill layers, grass-topped dirt ground with scrolling seams, gold ? blocks, green pipes with lips, spinning gold coins, the sailor, the goomba, the flagpole) rather than emoji `fillText` — this both looks crisper/more authentic AND sidesteps the documented sandbox canvas-emoji-contrast quirk entirely, so the game now renders identically well in the headless sandbox and on real devices. The `#runCv` CSS background was switched from the watercolor village image to a plain sky-blue fallback since the canvas now paints its own complete opaque platformer scene. Particle popups (`+1`, `+3 🍄`, `💥`) now render with a dark outline for legibility over the bright scene.
    - Economy/finish wiring unchanged: coins → `ws.kronor` (×3) + capped once-daily `minigame` point log; the flag is the primary finish trigger with a `dist >= TARGET+600` safety net. `runGameStep` early-returns once `finishing` is set so driving it in a loop (the test) can't crash mid-finish. Verified: full 22-file regression suite clean, and Playwright playthrough in real Chromium confirmed the sky/clouds/hills/ground, an airborne jump, a ? block being hit and releasing a mushroom (+3), a green pipe, the goomba, and the flagpole finish → summary ("19 mynt samlade — 57 kr … +4 p"), all with zero page errors.
- **Krokfiske (hook-fishing) reworked into an authentic mobile-fishing-game view (added 2026-07-16, follow-up):** matching the "make the games more authentic" direction (and the original mobile-fishing-game screenshots the user shared). The first cut drew fish/hazards as emoji over the illustrated dock backdrop; rebuilt `FISHG`/`startFishGameLoop`/`fishGameStep`/`fishGameDraw` into a proper vertical cross-section scene, all original canvas-drawn artwork (no emoji `fillText`, so it renders crisply in the sandbox too):
    - **Vertical water column** — sky band with drifting clouds up top, the **fishing boat drawn on the surface** (hull, cabin, windows, rod) bobbing gently, the **line drawn from the rod tip down to a J-shaped hook** with a golden bait glint that turns red while "stuck", water that **darkens with depth** (light blue → deep navy), animated surface waves, faint light shafts, **kelp swaying at the bottom**, and bubbles rising from the hook.
    - **Species-typical depths** via new `fishDepthFrac(sp)` — cheap surface shoal fish (strömming/abborre) up top, the valuable ones (torsk, lax) down deep, so chasing value means diving toward the bottom where the junk (`HAZARD_TYPES` tire/jelly/weed, drawn as canvas shapes now, hovering in mid-to-deep water) guards them — the genre's "worth more the deeper you dare" tension, which also makes the rod upgrade (bigger hook radius) matter most exactly where it's hardest.
    - **Fish drawn per-species** (`FISH_LOOKS` / `drawFishShape`): body + tail + top fin + eye, with species coloring — silvery strömming, green striped abborre, long green gädda, spotted torsk, salmon-pink lax — each flipped to face its swim direction with a gentle wobble.
    - **Catch popups now show real money** (`+N kr` from `sellPrice(species)`, rare catches in gold) and a running **kr total in the HUD**, tying the moment-to-moment game directly to the market economy. Every economy hook is otherwise unchanged (`rollSpecies` rodLevel gating, `addCatch`, once-daily rare "storfångst" bonus, fuel/sails session length) — the collision detection itself (`hookR+fishR` distance test) is byte-for-byte the same as the previously-verified version.
    - Verified: `node --check`, full 21-file regression suite clean (`test_trader.js`'s `fishCaught`/`hazardHit`/mid-session-re-render assertions still pass — the internal state field names they touch were preserved), and Playwright in real Chromium confirmed the boat/line/hook/depth-gradient/kelp/bubbles all render, a held-drag sweep actually catches fish (3 caught, "+4 kr"/"+7 kr" popups, "15 kr" HUD total), zero page errors. (Screenshot-harness note for future sessions: to catch fish via Playwright you must `mouse.down()` and keep the button held while moving — the hook only follows `pointermove` when `e.buttons` is set; a buttonless `mouse.move` sweep catches nothing and is a test artifact, not a game bug.)
- **Fishing net-casting + rod integration + points for both mini-games (added 2026-07-17):** requested as "add the casting of fishing net to the fishing game and incorporate the better/upgraded fishing rods etc into its mechanism" plus "Also fishing and running to town gives points."
  - **Net casting (unlocks with the existing 🕸️ nät upgrade):** the workshop `net` upgrade — previously *only* passive over-time income (`netAccrued`/`collectNet`) — now *also* lets you cast the net during a fishing trip. New `castFishNet()`/`fishNetReady()` + a `FISHG.net` state: a "🕸️ Kasta nätet" button appears below the fishing canvas only if `ws.net` is owned (otherwise a hint nudges you to buy one in Verkstaden); casting opens a widening circular mesh net from the hook's current position that scoops **every fish it sweeps over** (ignores junk), on a 7s cooldown (button shows a live "🕸️ Nät (Xs)" countdown, patched in the same 250ms tick path that refreshes the timer without rebuilding the canvas). Verkstaden's net copy updated to advertise the cast. The net catch reuses the same `fishCaught` path (kr + storfångst bonus), so it's a big-burst alternative to precise hooking, rewarding both owning the net and positioning over a shoal.
  - **Rod upgrades now visibly matter in the moment-to-moment game** (before: only hook radius + species gating, both invisible): new `fishFollowMs(rodLevel)` makes the hook track your drag **snappier at higher tiers** (rod 0 → 95ms ease, rod 3 → 53ms — real "better control" feel), the drawn **hook and bait scale up** with rod level, and the current **rod tier name is shown** in the fishing panel ("🎣 Kolfiberspö · Denna tur: N fångster"). The pre-existing `fishHookRadius` (bigger catch zone) and `rollSpecies` rodLevel gating (finer/deeper fish unlocked) are unchanged — now they're legible rather than hidden. Rod/net ownership is cached in `FISHG.rodLevel`/`FISHG.hasNet` at session start (can't change mid-trip).
  - **Points for fishing (once per day), matching the runner:** new `fishSessionPts(caught)` (2 fish → 1 p, capped at 4) offered as a "Bokför +N p" button on the FISKETUR SLUT summary, logged under `minigame`/`fiske:<date>` — a *distinct* daily ref from the town-runner's `marknad:<date>`, so **both mini-games can be banked the same day** on top of the kronor economy and the rare-catch storfångst bonus. (The run to town already awarded points via its summary; this brings fishing to parity.)
  - **Real routing bug found and fixed via the new test:** the FISKETUR SLUT summary (and thus the new points button) was only reachable after a *beach* trip — `renderBryggaPanel` showed the bryggan intro (not the summary) whenever `!fishSession`, so a *boat* trip's end skipped straight past the summary and the points could never be banked. Fixed with a `fishSummaryPending` flag (set at `endFishSession`, cleared by `traderShowPanel` when the crew leaves the landmark) that routes brygga back through `renderFishUI`'s summary until the player acts. Caught precisely because the test drove the boat-mode end-of-trip flow rather than assuming it mirrored beach mode.
  - Verified via a new `test_fishing_net_points.js` (rod/points pure-function values; net locked without ownership + the Verkstaden nudge shown; net unlocks, casts once, then blocks on cooldown; rod tier displayed; a boat trip's end shows the point-log button and banking it writes a `fiske:<date>` minigame log distinct from `marknad:<date>`) plus the full 22-file regression suite clean. Playwright in real Chromium confirmed the net button + cooldown, the widening mesh-net cast animation over the water scene, the rod tier line, and the "5 fångster — 53 kr … Bokför +2 p" summary, zero page errors.
- **"Känner du igen den?" — image-identification checks in the pop-quiz (added 2026-07-17):** requested as "add to pop quiz checks of identifying seen birds, flora to make sure user actually knows what they look like - show image of bird from marked as spanade and give 3 options. -3 points for missed or wrong answers." Folded into the existing pop-quiz mechanism (same daily cap/cooldown/ref-counter system from the earlier minus-points work) rather than a parallel system, so it's just a different render branch some rounds take.
  - **Only quizzes on species the sailor actually spotted:** new `mySpottedImgQuizPool()` scans `seabirds`/`gulls`/`waders`/`flora` (birds + flora, per the request — not vessels/insects/etc.) and keeps only items with a real `hasLog(me.id,"spot",cat+":"+idx)` entry. A handful of non-species entries bundled into those categories (a red cottage, a windmill, a church spire, a lighthouse, "gull on a pole" as a joke free-point card, etc.) are excluded via a new `noId:true` flag added directly on those `GAMES` items — explicit and precise rather than guessed at with name-pattern heuristics.
  - **Real photo via Wikipedia, always with a graceful fallback:** `fetchSpeciesImage(title)` calls the public `sv.wikipedia.org` REST summary API for a thumbnail; `imgQuizTitle(item)` strips descriptive qualifiers first ("En (buske)"→"En", "Tall på häll"→"Tall") so lookups target the clean species name. If the fetch fails, times out, hits a disambiguation page, or has no image (or `fetch` doesn't exist at all, as in this project's jsdom test harness — confirmed offline-safe, matching the established pattern from the wind-forecast feature) — the round silently falls back to a normal text pop-quiz question instead of ever showing a broken or empty quiz. The `<img>` tag also carries its own `onerror` hide, so even a mid-load failure degrades cleanly. **Verified this sandbox can't reach `sv.wikipedia.org` at all** (same outbound-network restriction pattern as the previously-documented Open-Meteo/CDN blocks) — real user browsers reach it normally.
  - **3 options, same-category distractors:** `imgQuizDistractors(cat,correctName,n)` pulls 2 other names from the *same* `GAMES` category as the correct answer (e.g. a real seabird mixed with other seabirds, not a random flower) — a genuine identification challenge rather than a trivially-guessable one.
  - **Right +3, wrong OR skipped ("missed") −3 — deliberately different from the plain-text pop-quiz's skip behavior**, which stays free exactly as it was (this session's earlier minus-points work wasn't asked to change, and didn't). The "Inte nu" button on this quiz type is relabeled "Inte nu (−3 p)" so the cost is disclosed before the sailor decides to skip, not sprung on them after. Logged under the same `quiz`/`pop:<date>:<n>` ref space as every other pop-quiz round, so it counts toward the shared daily cap and can't be gamed by skipping repeatedly to avoid it.
  - Verified via a new `test_img_id_quiz.js` (title-stripping unit tests; `fetchSpeciesImage` resolves `null` offline without crashing; the spotted-only pool starts empty and gains exactly one entry after a real spot-log; `noId` entries never enter the flora pool; distractors are same-category and distinct; direct `renderImgQuiz` runs for right/wrong/skip each logging the correct ±3; `maybePopQuiz` still renders a normal question when no image is available) plus the full 23-file regression suite clean. Playwright in real Chromium confirmed the card renders correctly (3 clean same-category options, the upfront "−3 p" skip warning) and the full right/wrong feedback flow (red/green highlight, explanation with the correct answer named, the "-3 p — kolla upp den bättre nästa gång!" toast) — all with zero page errors, image-load failure included.
- Ranks: Jungman 0 → Lättmatros 30 → Matros 60 → Båtsman 130 → Styrman 220 → Skeppare 320 → Kommodor 420.
- **Captain:** each boat has its own `boats.captain` (name match, case/space-insensitive) → sees "Ändra ruttordning" on board; reorder syncs live to that boat's crew only (goal harbor locked as last stop). Challenges keyed by harbor NAME (`cp:<name>`) so they follow reordering. Sun-Day's captain is Martin; new boats auto-captain their creator.
- UI: game HUD (avatar+rank chip → profile; gold ⭐ points coin; blue ⛵ crew coin), XP bar to next harbor, wind bar (Open-Meteo, cached offline, tap to refresh), bottom nav (5 + Mer sheet), splash screen, wave-sweep view transitions, confetti/floating-points/pulse juice.
- Views in Mer: Skeppschatt (realtime; @gemma → gemma_bridge.py), Galleri (logbook photos), Törnrapport (per-day sailing stats from track_points + per-member activity + crew totals), Navigationsskolan, Byssan (8 recipes), Frågesport, Läten (audio players w/ external fallback), Sjötermer (diagrams + boat specs), Live AIS (Fintraffic, often CORS-blocked → fallback text), Seglandet 2026 (harbor guide + Leaflet map).
- Map (Seglandet): Leaflet + OSM, markers for 14 PLACES + Storören; overlays: Väylävirasto WMS fairways (default ON — **layer names `vaylat,vaylaalueet` unverified**, check they render!) + OpenSeaMap seamarks. Buttons: ⛶ fullscreen, 📍 follow (live position+speed), ⏺ track recording (batches to `track_points` every 45 s), 🛤️ today's track replay, 📡 share position (upserts to `boat_positions` every ~45 s), 🚢 show boats (plots every boat's last shared position, realtime). Offline fallback = schematic SVG.
- Harbor guide: 14 route harbors (fee+services+shared star-rating logbook with photo upload, client-side resize to 1280 px JPEG) + 9 alternatives + 17 more behind "+ Fler gästhamnar". Prices: Kasnäs from 34 €, Helsingholm 20 €, Högsåra 25+5 €, Ekenäs 25 €, Näsby 20 € all-incl <34 ft, Norrby 27 € (sourced); rest marked "ca".

## ⚠️ Open items / known issues

1. **UNVERIFIED FULL-FILE SYNTAX.** The Cowork sandbox's file mirror of `index.html` was frozen during the last work session, so the final ~10 edits (tracker, report, GPS unlock, facit buttons, map layers, Barösund) were verified only by isolated unit tests (all green) and inspection — NOT by a full `node --check` on the assembled file. **First action in a new session: extract the `<script>` block and run `node --check`, then fix any error.** Roope must also smoke-test locally (double-click index.html) before deploying.
2. ~~**Live site is stale.**~~ **Outdated as of 2026-07-15** — sunday-spaning.netlify.app now auto-deploys from `claude/game-improvements-review-rhb28k` to production on every push (confirmed via the Netlify API: the live deploy's `commit_ref` matches the latest commit within seconds of pushing). No manual folder-drag needed anymore.
3. **Bird sounds not bundled.** Cards look for `sounds/<scientific-name>.mp3` (e.g. `haliaeetus-albicilla.mp3`), fall back to Xeno-canto links. Xeno-canto blocks bots (Anubis) — files must be downloaded manually by Roope.
4. **Väylävirasto WMS layer names unverified** (see Map above).
5. **Jarvis vault sync pending.** Two `vault_remember` calls timed out (Gemma server offline 11.7). Full history is in Claude's auto-memory (`robert-sailing-game` memory) and mirrored here.
6. Pop-quiz answered wrong still writes a 0-point log (marks the day used) — intended, but crew may ask.
7. `logs.kind='bonus'` is allowed by schema but unused/not duplicate-protected.

## Conventions for future edits

- Single-file app; all JS in ONE `<script>` at the end of `index.html`. Function declarations (hoisted) — order rarely matters; top-level `const/let` data lives at the top of the script.
- All UI text in Swedish, playful nautical tone. Points refs: `spot:<cat>:<idx>`, `cp:<harborName>`, `knot:<i>`/`nav:<i>`, `quiz:<date>`/`pop:<date>`, `sur:<date>`, `<game>:<date>`.
- Supabase MCP is connected in Roope's Cowork — use `apply_migration` for DDL, `execute_sql` with `set local role anon` to test RLS (supabase.co is NOT reachable from the sandbox shell).
- Sandbox mirror of index.html can lag minutes behind edits: verify new logic in a fresh scratch `.js` file (they sync instantly), run with node, delete after.
- Roope's preference: save agreed decisions/learnings to the Jarvis vault (`vault_remember`), problems to Knowledge Base.

## Wishlist / not built

- Bundle bird mp3s (manual download) · PWA manifest + service worker for true offline install · export Törnrapport as PDF/share text · date picker for track replay · sound effects · deleting/moderating chat & logbook posts (currently insert-only from app).
