# HANDOVER — Skärgårdsspaning (sailing trip game)

**Purpose of this doc:** full context for continuing development in a new chat. Read this + skim `index.html` before making changes.

## What this is

Multiplayer deck game (PWA-style web app, Swedish UI) for Roope's sailing trip in the Turku archipelago, **departing Monday 2026-07-13**. Crew: Roope, Tiina, Pernilla (join by car at Hangö), Patrik & Martin (depart by boat from Storören). **Martin is captain.** Boat: s/y Sun-Day, Degerö 33 (LOA 9,81 m, beam 3,52 m, draft 1,5 m, mast 15 m, 5 200 kg, sloop).

Route (15 board tiles, editable by captain): STORÖREN → Barösund (Mon night, boat crew only) → Hangö (crew meetup "Mönstring") → Airisto → Själö → Brännskär → Korpoström → Aspö → Nötö → Björkö → Helsingholm → Högsåra → Örö → Dalsbruk → 🏁 KASNÄS (spa finale).

## Files (all in this folder)

| File | Role |
|---|---|
| `index.html` | The entire app (~2300 lines: CSS + HTML + one `<script>`) |
| `config.js` | Supabase URL + anon key (filled, working) |
| `schema.sql` | Full schema incl. `boats` — kept in sync with live migrations (last synced 2026-07-15) |
| `gemma_bridge.py` | Runs on Roope's home PC: polls chat, answers @gemma via local Ollama |
| `netlify/functions/mistral-fallback.mts` | **Added 2026-07-14** — Netlify Scheduled Function, cloud fallback for @gemma when the home PC is off (see below) |
| `boat.jpg` | Watercolor hero image (login screen) |
| `SETUP.md` | User-facing setup guide |
| `sounds/` (optional) | Not created yet — see Bird sounds below |

## Infrastructure (all live, all free tier)

- **Hosting:** Netlify site `sunday-spaning` → https://sunday-spaning.netlify.app (site id `ef94bbc0-ba0c-4443-b146-d582def9d81a`, team `soderhro`). **Updated 2026-07-14: now linked to GitHub** (`soderhro-source/Sk-rg-rds-spel`, branch `claude/game-improvements-review-rhb28k`) for auto-deploy on every push — the manual drag-the-folder workflow and the blocked npx-upload workaround are no longer needed, just `git push`. Base directory/build command/publish directory are all left empty (plain static site, no build step).
- **Backend:** Supabase project `wptovqfypslbneobnjim` (eu-west-2, "Söderholm's Org"), managed via the Supabase MCP connector. Tables: `boats` (name/emoji/captain/route — **added 2026-07-14 for multi-boat support**, see below), `sailors` (name+PIN hash+emoji+`boat_id` fk), `logs` (all points: kind ∈ spot/quiz/checkpoint/knot/surprise/minigame/bonus, unique indexes prevent duplicates; surprise unique per crew per day), `chat`, `port_notes` (harbor logbook incl. `photo_url`), `settings` (key/value jsonb — legacy singleton `captain`/`route` rows kept but no longer read by the app; route/captain now live per-boat on `boats`), `track_points` (GPS log: lat/lng/sog/ts). Storage bucket `photos` (public, anon upload). RLS: open for anon (friends-level security). Realtime enabled on logs, chat, settings, boats.
- **Auth:** name + 4-digit PIN, SHA-256 client-side. No email.
- **Offline-first:** localStorage write-queue for logs, auto-flush every 15 s + on `online`. Chat/photos/map need signal.

## Game design (current state)

- Candy Crush-style saga board, 15 nodes serpentine. **Everyone starts at Storören**; tile = `floor(points/30)` capped at 14; **MÅL at 420 p**. Passed harbors = green + ⭐, current = pulsing gold ring, fog-of-war ("— ? —") beyond next harbor. Crew ship ⛵ moves on sum of a **boat's own crew's** points (leg = 30 × that boat's crew size).
- **Multi-boat (added 2026-07-14):** friends on a second boat can join the same game. At "Mönstra på" you pick an existing boat or create a new one (name+emoji; creator auto-becomes its captain). Each boat has its own `route`, `captain`, saga-board position and leaderboard (`mySailors()`/`myBoat()` scope everything); the board tab shows a compact "Andra båtar" line with each other boat's points/position for a friendly-race feel. Species spotting/quiz/trivia/arcade stay global fun, not boat-scoped. `trkShowToday`/Törnrapport filter `track_points` to the viewer's own boat's sailor IDs so two boats' GPS tracks don't get mixed on the map. A boat with no captain shows "Bli kapten" to whoever's viewing it (self-serve, no pre-coordination needed). Seed boats: `Sun-Day` (captain Martin, original route) and `Vänbåten` (placeholder name/emoji — no captain yet, first joiner should rename via Supabase or just claim captain and keep the name).
- **Known gap:** the map (Seglandet tab) does not yet show live position markers for *other* boats — it only ever showed the viewing device's own GPS dot (this predates multi-boat). Real-time cross-boat position sharing on the map would need a new realtime subscription + colored markers; not built, flagged as a possible follow-up.
- **Crew roles (added 2026-07-14):** captain can assign crew to 6 roles (`ROLES` const: Rorsman/helmsman, Skeppskock/cook, Maskinchef/engineer, Ankringsmästare/anchor master, Utkik/lookout, Navigatör/navigator) via a panel in the 🎁 Dagens tab — reassignable anytime, stored in `boats.roles` (jsonb map roleKey→sailorId). Whoever holds a role sees "Din roll idag" with a rotating daily task (deterministic by date+role hash, `ROLE_TASKS` pool of 3 per role, same pattern as `SURPRISES`/`POPQUIZ`) and can log it once/day for 3 p (`kind='bonus'`, `ref='role:<roleKey>:<date>'`; DB-enforced via the `one_per_sailor` unique index, which now also covers `'bonus'`). Only the assigned holder can log their own role's task — no gating on other content. Role badges show next to names in the board leaderboard. Captain (the role, not one of the 6) is unchanged — still `boats.captain`/`claimCaptain()`, separate mechanism.
- Points sources: species spotting (~90 cards, 6 categories in one 🔭 Spana tab with chips, each card has 📷 facit Google-Images link), harbor challenges (+5; unlock by points OR **GPS within 1.5 km**; Storören "Avfärdskontroll" open from start), knots (6), navigationsskolan (6 lessons, kind='knot', ref='nav:i'), quiz (bookable 1×/day), daily pop-quiz overlay (+2, ship+seamark theory, 16 Qs), daily surprise (first-to-claim crew-wide), arcade: Lastrummet/Tetris ≤5 p/day, Skärgårdsminne/memory ≤4, Bojkrossen/match-3 ≤5, Ålen/snake ≤4, Uno-poäng (no game points — see below).
- **Sjömansvisor playlist (added 2026-07-14):** new 🎵 tab in Mer (`SONGS` const, 8 entries — Evert Taube classics + trad. sjömansvisor) linking to YouTube search results (not specific video URLs, so links never rot). Same `.scard` styling as Läten.
- **Uno-poäng (added 2026-07-14):** 5th tile in 🕹️ Spelrum, requested by Patrik — a standalone score reporter for the physical card game Uno, unrelated to the sailing game's point system (no `logs` entry, no daily cap). Add players, enter each round's penalty points (loser's remaining-card values; standard Uno scoring explained in the intro text), running totals sorted lowest-first (leader gets 👑). Persisted client-side only (`localStorage.ss_uno`) — not synced to Supabase, so it's per-device, resettable, and 100% independent of the trip scoring.
- **Skepparens loggbok / captain's log (added 2026-07-14):** new `captain_log` table (boat_id, sailor_id, departure/arrival place+time, miles, weather, notes), captain-only entry form inside the existing 📜 Törnrapport tab (`renderCaptainLogCard()`), scoped per boat like everything else post-multi-boat. Distinct from `port_notes` (the per-harbor star-rating logbook) — this is a proper leg-by-leg ship's log: who logged what, when they left/arrived, distance, weather (free text + quick chips), freeform notes. "Fyll i från spår" button pre-fills distance from the day's GPS track if available. Only the boat's captain sees the add/delete controls; entries are visible to the whole crew. **Bug fixed during this build:** the delete handler originally re-rendered the card before removing the old one, leaving two elements with the same `id` in the DOM simultaneously — invalid HTML that silently broke a scoped `querySelector` (caught by the jsdom smoke test, not by `node --check`, since it's syntactically valid JS). Fixed by removing the stale card first.
- Also fixed two latent boat-scoping bugs in `renderReport()` left over from the multi-boat change: the per-sailor breakdown and the "ship reached" tile were still reading the *global* `sailors` array instead of `mySailors()`, which would have mixed both boats' crews into one boat's report.
- **GPS auto-resume + wake lock (added 2026-07-14):** "⏺ Logga färd" preference now persists (`localStorage.ss_trk_rec`) and silently resumes (`trkResumeIfWanted()`, called from `enterApp()`) next time the app opens or a different sailor logs in on the same device — no more re-tapping every session. While recording, requests a Screen Wake Lock (re-acquired on `visibilitychange`) so the screen doesn't sleep mid-log. **Hard limit, by design of the web platform:** this is still foreground-only — a plain web app cannot log GPS while the browser tab is fully closed or the phone is locked/backgrounded for long, on iOS especially (no reliable background geolocation for web apps, PWA or not). Auto-resume removes the "forgot to tap record" friction; it does not make tracking work with the app closed. `logout()` now clears the active `watchPosition` (was a latent bug: pre-fix, staying "recording" through a logout would queue `track_points` rows with `sailor_id:null`).
- **Planeringsläge / planning mode (added 2026-07-14):** `canPlan()` = captain OR whoever holds the Navigatör role — both now see the board tab's route button (relabeled "🧭 Planeringsläge", was captain-only "Ändra ruttordning"). No new table; reuses `boats.route`. The old reorder-arrows UI is still there, now wrapped with: a short wind forecast (`windForecastAt()`, Open-Meteo `hourly` params, centered on current GPS fix or the next harbor if no fix) and a "next stop" card showing live distance/ETA (`distEtaTo()`, haversine from `lastFix` to the harbor's known coordinates, editable assumed speed input) plus a "🔀 Byt ut mot en annan hamn" swap picker pulling from `ALT_HARBORS`+`EXTRA_HARBORS`, sorted nearest-first when a GPS fix exists. Swapping only applies to non-goal slots (index 13 / NÅDENDAL stays locked, same as before). Changes sync live via the existing `saveRoute()`.
- **Alt/extra harbor coordinates researched (added 2026-07-14):** `ALT_HARBORS` (9) and `EXTRA_HARBORS` (15) now carry real `lat`/`lng` for 22 of 24 — verified via web search (official harbor sites, vierassatamat.fi, gasthamnar.fi, Navily, etc.), not guessed; one cross-check landed within 1.3 km of the already-verified `PLACES` entry for the same harbor (Nådendal), which gave good confidence in the sourcing. The 2 that couldn't be verified (**Kirjais**, **Villa Wolax** — address known, no dock-level coordinate published anywhere indexed) were deliberately left without `lat`/`lng` rather than estimated; the planning-mode swap picker shows "avstånd okänt" only for those two. `harborCoords()` now searches `PLACES`+`ALT_HARBORS`+`EXTRA_HARBORS` (was `PLACES`-only), so GPS-based harbor-arrival unlock (`nearHarbor()`) also now works if a captain/navigator swaps in one of these as a route stop.
- **Mobile overflow fix (added 2026-07-14, Roope reported elements going off-screen):** `html,body{overflow-x:hidden}` added as a global safety net (was missing entirely — any single overflowing child could scroll the whole page sideways). Root causes fixed rather than just masked: (1) the captain's-log "Distans" row had a fixed-content button (`#clogFillMiles`) next to a `flex:1` input with no `flex-wrap` — classic mobile trap, flex items don't shrink below content size by default; added `flex-wrap:wrap`+`min-width:70px`. (2) The Uno score `<table>` had no `table-layout:fixed`, so a long player name could grow the table past the viewport; fixed with `table-layout:fixed` + explicit column widths. (3) `#clogFrom`/`#clogTo`/`#clogWeather`/`#clogNotes` had no `maxlength` (every other text input in the app does) — added 40/40/60/300. (4) `.cpCard .t` (used for harbor-swap cards and log entries, both with a `float:right` badge) got `overflow-wrap:anywhere` so long unbroken text can't force width. Verified empirically with Playwright + real Chromium at 320px and 390px viewports — every tab, the boat-creation flow, and a deliberate stress test with an unbreakable 50+ char boat name, all checked via `scrollWidth` vs `clientWidth`: zero overflow after the fix (jsdom can't catch this class of bug at all, since it doesn't run CSS layout — worth remembering for future layout work).
- **More tap/switch animation polish (added 2026-07-14):** the app already had a lot of juice (wave-sweep tab transitions, confetti, floating +points, board ship bob, current-tile pulse ring, splash sail animation, `prefers-reduced-motion` respected globally) — this pass filled concrete gaps rather than re-doing what existed. `.arcBtn`, `.sailorBtn`, `.emojiRow button`, `.padRow button` all had the raised "3D button" drop-shadow look implying press-ability but **zero `:active` press feedback** — tapping did nothing visually. Added matching press-down transforms to all four. Added `.cpCard.tapCard:active{transform:scale(.97)}` scoped only to the genuinely-clickable planning-mode swap cards (deliberately did *not* add whole-card press feedback to the default `.cpCard`, since most instances — role rows, captain-log entries — have buttons *inside* them rather than being clickable themselves; a card-wide press effect there would misleadingly suggest the whole card is tappable). The crew-points coin (`#hudCrew`) now pulses on increase, mirroring the existing personal-points pulse (`_lastCrewPts` tracking added to `updateTop()`, same pattern as `_lastPts`). Tab switching gets a keyframe bounce (`tabHop`/`tabIcPop`) instead of an instant snap — needed a `@keyframes`-based approach rather than `transition`, since `buildTabs()` destroys and recreates the tab elements every render, so there's no persisting element for a CSS transition to interpolate from. Verified via Playwright: transitions present on all four button classes, tab bounce animation applied, crew-coin pulse fires on a real score change, and — importantly — everything correctly goes to `0s`/none under `prefers-reduced-motion: reduce` via the pre-existing global media query.
- **Mistral cloud fallback for @gemma (added 2026-07-14):** `netlify/functions/mistral-fallback.mts`, a Netlify **Scheduled Function** (`schedule: "*/2 * * * *"`, min interval Netlify allows is 1 min) that runs independently of Roope's home PC — this is the key difference from just adding a fallback *inside* `gemma_bridge.py`, which was considered and rejected: if the fallback lived only in the Python bridge, it would never run when the PC that hosts it is off, defeating the point. The function polls the same `chat` table (same duplicate-detection via `reply_to`, same system prompt/persona as the Python bridge) and only answers an `@gemma` question if it's been unanswered for **3+ minutes** (`GRACE_MS`) — long enough that the local Gemma bridge, when the PC *is* on, always gets first crack at it (it normally replies within ~15s) — and gives up on anything older than 30 minutes (`STALE_MS`), so it won't dredge up ancient unanswered questions. Answers post under the same `"⚓ Gemma"` author for a seamless crew experience (no visible distinction between "home Gemma" and "cloud Mistral" answers — a deliberate simplicity choice, not an oversight). Uses `mistral-small-latest`.
  - **Security/cost note:** the Mistral API key is read via `Netlify.env.get("MISTRAL_API_KEY")` — set only in Netlify's project environment variables (Site configuration → Environment variables), never committed to this repo or shipped to the public site. This matters more here than the Supabase anon key situation: an exposed *paid* third-party API key on a publicly reachable site (no real auth beyond PIN) would be a genuine abuse/cost-exposure risk, unlike the Supabase key which is protected by RLS and already an accepted trust tradeoff. **The key has not been set yet** — until someone adds it via the Netlify dashboard, the function safely no-ops every run (checked and logged, doesn't error) and the game works exactly as before with only the home-PC Gemma. Setup steps are in `SETUP.md`.
  - **TEMPORARY change (2026-07-14, same day):** `GRACE_MS` dropped from 3 min to 5 s and the schedule tightened from every 2 min to every 1 min (Netlify's minimum), on request — real chat data showed 3-day-old `@gemma` questions still unanswered by the home bridge, so it's effectively Mistral-primary right now rather than a true fallback. **Cannot actually "turn off" `gemma_bridge.py` from here** — it's a script on Roope's physical machine with no remote control surface; if it's not running, there's nothing to turn off, and if it ever *is* running again, it'll still occasionally win the race on a 5s grace window (harmless, just a nice bonus, not a conflict — duplicate-answer protection via `reply_to` still applies). To restore "wait for local Gemma first" behavior later, bump `GRACE_MS` back to `3*60*1000` (or similar) and the schedule back to `"*/2 * * * *"` — both are called out with inline comments in the file.
  - **Confirmed working live (2026-07-14):** verified end-to-end against the real Supabase `chat` table, not just mocked tests — Roope's real question "hur långt är det till nådendal?" got a real, coherent, in-character Mistral answer ("⚓ Gemma", ~25 nm / 5h at 5kn, with the expected sjömanshumor) in **~57 seconds** from when the correctly-keyed deploy went live. Root-caused an earlier false alarm (7 min with no answer) to env vars only taking effect on the *next* deploy after being added, not retroactively on already-running functions — worth remembering for any future env var troubleshooting here.
- **Diskat idag? / dishes chore points (added 2026-07-14):** small addition to the 🎁 Dagens tab, same visual pattern as the role-task card — `DISH_PTS=2`, `kind='bonus'`, `ref='dishes:<date>'`. Deliberately **not** role-gated (unlike role tasks) — anyone aboard can grab it, once per person per day, no captain/role assignment needed. No schema change; reuses the `bonus` kind and the `one_per_sailor` unique index that already covers it (added for roles). Verified via jsdom: card renders, logging awards +2 p and flips to the done state, button disappears after use.
- **"Diskmästaren" leaderboard in Törnrapport (added 2026-07-14):** counts each sailor's `dishes:*` bonus logs (`dishCount()`, filters `ref.startsWith("dishes:")` — distinct from role-task bonus logs which use a `role:` prefix, both share `kind='bonus'`). Adds a small standalone 🍽️👑 card near the top of the report naming whoever has the most (ties shown together, e.g. "🐟 Roope & 🏴‍☠️ Pati"), only rendered if at least one sailor has logged dishes, plus runner-ups below in a dimmed list. Also added a "🍽️ Diskat" count row to each sailor's existing per-person stat card. Verified via jsdom with 2 sailors at 3 vs 1 logged days: champion card shows the right person/count, per-sailor rows show correctly.
  - Verified by running the actual `.mts` file (not a reimplementation) via `node --experimental-strip-types` with mocked `fetch`/`Netlify.env`: confirmed it correctly answers only a stale (4-min-old) unanswered question, and correctly skips a too-fresh (1-min) one, an already-answered one, a 40-min-stale one, and a non-`@gemma` message — plus a separate run confirming the graceful zero-op behavior when `MISTRAL_API_KEY` is unset. Could not test the actual Netlify Scheduled Function runtime/cron trigger itself from the sandbox (no Netlify CLI/runtime available here) — worth a real smoke test once a key is configured and deployed.
- **New-boat setup wizard + per-boat trivia bug fix (added 2026-07-15):** requested as "an initial travel wizard when a new boat is set up during mönstring." Two parts:
  - **Real bug fixed:** a single hardcoded `const BOAT={...}` held Sun-Day's specs (model/LOA/beam/draft/mast/weight/rig) and was used both for the Sjötermer boat diagram and for 8 hardcoded trivia questions (3 in `QUIZ`, 5 in `POPQUIZ`) — meaning **every boat's crew, including `Vänbåten`, was quizzed on Sun-Day's numbers.** Replaced with per-boat spec columns on `boats` (`model`/`loa`/`beam`/`draft`/`mast`/`weight`/`rig`, all `text`, all nullable) and `myBoatSpecs()` (reads the viewer's own boat, gracefully returns `null` for any unset field). Sjötermer (`renderTerms()`) and the boat SVG diagram (`sloopSVG()`, which also had "Sun-Day" hardcoded into its `aria-label` and in-diagram text) now read the viewer's actual boat name/specs, with fallback text ("Båtens mått är inte inlagda ännu.") when a boat has no specs set — this matters because `Vänbåten` predates this feature and has no specs, by design (no retroactive-edit UI was built).
  - **Dynamic per-boat trivia:** the 8 hardcoded Sun-Day questions were deleted from `QUIZ`/`POPQUIZ` and replaced with `boatSpecQuestions()`, which generates up to 6 multiple-choice questions (model/draft/mast/beam/weight/rig) **only for specs the viewer's boat actually has set** — skips silently otherwise, so a spec-less boat like `Vänbåten` gets zero boat-spec questions rather than blanks or errors. Distractors are drawn from fixed pools (`BOAT_MODEL_POOL`, `BOAT_LOA_POOL`, etc., 4-8 plausible values each) via `pickDistractors()`/`mcQuestion()` — a deliberate design choice (confirmed with the user rather than assumed) to use dropdown-select pools instead of freeform text inputs in the wizard, specifically so distractor generation never has to parse arbitrary user-typed strings with mixed units/formatting. `allQuiz()`/`allPopQuiz()` (== static array + `boatSpecQuestions()`) replace the old direct `QUIZ`/`POPQUIZ` references in `startQuiz()`/`maybePopQuiz()`.
  - **The wizard itself:** the "➕ Ny båt" branch of the mönstring form (`newSailorForm()`, `#nsNewBoatFields`) now has, beyond the existing name+emoji: a starthamn `<select>` (defaults to "Helsingfors (Katajanokka)", stored as the literal `"HELSINGFORS"` to match the existing special-case in `harborCoords()`) and a målhamn `<select>` (defaults to "Nådendal (Naantali) — Målgång!", stored as the literal `"NÅDENDAL"` to match the existing `HARBOR_CH` key and keep the celebratory finish-line flavor text) — both pull their non-default options from `allHarborNames()` (dedup+sorted union of `PLACES`+`ALT_HARBORS`+`EXTRA_HARBORS` names), confirmed with the user as "pick from an existing list" rather than freeform text. Plus 7 boat-spec `<select>` pickers (model/loa/beam/draft/mast/weight/rig), each pool-backed with a blank "— välj —" default (so skipping a field stores nothing rather than silently picking the pool's first option) and an "Annat (skriv själv)" escape hatch that reveals a freeform text input (`specFieldHtml()`/`wireSpecField()`/`specFieldValue()`) for boats that don't match any pool value. Per the user's explicit choice ("default route, edit later via Planeringsläge"), the wizard does **not** ask for the 13 middle stops — `route` is computed as `[...DEFAULT_ROUTE.slice(0,13), chosenEndHarbor]`, and the existing Planeringsläge harbor-swap picker remains the way to customize the middle stops afterward. `.field select` had no CSS rule before this (the app had never used a `<select>` anywhere) — added alongside `.field input`.
  - **Threaded the boat's own start harbor through everywhere `"HELSINGFORS"` was hardcoded:** `setRoute()` now takes an optional `startHarbor` param (`TILE_SHORT=[startHarbor||"HELSINGFORS",...ROUTE]`), `applyMyBoatRoute()` passes `b.start_harbor`, and the "other boats" comparison line in `renderBoard()` was also fixed (`[x.start_harbor||"HELSINGFORS",...x.route]` instead of always `["HELSINGFORS",...x.route]`) — this was a second, smaller instance of the same Sun-Day-only-assumption bug pattern, caught while auditing every `TILE_SHORT`/`"HELSINGFORS"` reference.
  - Supabase migration applied live: `alter table boats add column start_harbor/end_harbor/model/loa/beam/draft/mast/weight/rig text` (all nullable), then backfilled Sun-Day's row with its real specs (`start_harbor='HELSINGFORS'`, `end_harbor='NÅDENDAL'`, `model='Degerö 33'`, etc.) so its Sjötermer/trivia content is unchanged from before this feature. `Vänbåten` was left with all 9 fields `NULL` (predates the wizard). `schema.sql`'s `boats` table and seed `insert` updated to match.
  - Verified via a new jsdom test (`test_boat_wizard.js`): wizard fields render on the new-boat form; a boat created with custom specs shows those specs (not Sun-Day's) in Sjötermer and gets boat-spec trivia questions that reference it by name; a boat created with a custom start/end harbor gets the right `route`/`start_harbor`/`end_harbor` and the board caption reflects the custom start harbor; a boat created with zero specs filled in degrades gracefully (fallback Sjötermer text, no boat-spec quiz questions, no crash) — plus the full existing regression suite (`test.js`–`test6.js`, `test_dishes.js`, `test_dishking.js`) re-run clean.
- **⚙️ Kaptensrutan — consolidated captain settings page (added 2026-07-15):** follow-up request after the boat wizard: "add [a boat] rename option for the captain, but I also think [the] captain needs [their] own settings/overview page." User picked all four proposed contents (boat details, crew roles, route/Planeringsläge, transfer captaincy) and placement as a new tab in the Mer sheet. New nav entry `["kapten","⚙️","Kaptensrutan"]` in `NAV_MORE`, `renderKapten()` renders into `#kaptenBox`.
  - **Visible to everyone, editable by the captain only** (per the user's explicit choice): non-captains see a read-only summary (boat emoji/name, specs line, start/goal harbor, current captain, read-only role-holder list, a "Bli kapten" button if the boat has no captain yet). The captain sees the full edit form instead.
  - **Boat details, now editable after creation (the rename ask):** name, emoji, start/goal harbor `<select>`s, and all 7 spec fields — the exact same picker UI as the "Ny båt" wizard, reused via `specFieldHtml()`/`harborSelectHtml()` now generalized to take a `prefix` param (`"ns"` for the mönstring wizard, `"kap"` here) and an optional `current` value to pre-select/pre-fill from the boat's existing data, including falling into the "Annat" freeform slot if a stored value doesn't match any pool option. One "Spara båtuppgifter" button persists name/emoji/harbors/specs together (`saveBoatDetails()`); renaming re-checks the `boats.name` unique constraint against other boats (excluding itself) client-side before saving, matching the same check the wizard already does at creation time. Editing the goal harbor only replaces the last slot of the existing `route` array (`[...route.slice(0,13), newEndHarbor]`) — it does not touch the 13 middle stops, so any prior Planeringsläge customization survives. After a save, `setRoute()`/`refreshWhoAmI()` are called explicitly so the new name/start-harbor take effect immediately in the current session (`TILE_SHORT[0]`, the `#whoami` header) without needing a reload — `refreshWhoAmI()` is a small new extraction from what `enterApp()` used to do inline, so it can be called again after an edit.
  - **Crew role assignment — moved, not duplicated:** the picker (`renderRoleAssignPanel()`, extracted from what used to be inline in `renderSur()`) that lets the captain assign the 6 roles to crew now lives only in Kaptensrutan; it's gone from the 🎁 Dagens tab (which was "a bit buried," per the user's own framing of the problem). Dagens still shows "Din roll idag" (today's task + the log button) for whoever holds a role — that's role *use*, not role *assignment*, and stayed put.
  - **Route/Planeringsläge — folded in, not duplicated:** the "🧭 Planeringsläge" button is gone from the board tab (`renderBoard()`); an "🧭 Öppna Planeringsläge" button in Kaptensrutan opens the exact same `routeEditor()` overlay as before (wind forecast, next-stop ETA, harbor swap picker) — none of that logic was rewritten, just re-triggered from a new place. **Gated on `canPlan()` (captain OR navigator), not `isCaptain()`** — this one caught a real regression during testing: an early version only showed the button inside the captain-only edit branch, which would have taken away the navigator's existing ability to open Planeringsläge (previously available via the board button to anyone with `canPlan()===true`). Fixed by rendering the route section independently of the captain/non-captain branch, keyed on `planner=canPlan()` instead.
  - **Transfer captaincy (the fourth requested piece, previously not possible at all):** `claimCaptain()` was refactored into a shared `setCaptain(name,toastMsg)` helper; `claimCaptain()` now calls `setCaptain(me.name,...)` and a new `transferCaptaincy(sailor)` calls `setCaptain(sailor.name,...)`. Kaptensrutan lists every crew member on the boat with a "Gör till kapten" button (hidden for whoever already holds it) — since captaincy is matched by name string (`boats.captain`, case/space-insensitive, same mechanism as before), transferring is just overwriting that string, no schema change needed.
  - Verified via a new jsdom test (`test_kaptensrutan.js`): board tab no longer has `#routeEd`, Dagens no longer has the role-assignment list, captain's Kaptensrutan shows a prefilled editable name field plus role/route/transfer sections, renaming a boat updates both the stored boat record and the `#whoami` header live, Planeringsläge still opens correctly from its new location, a non-captain gets the read-only view with no edit fields, and transferring captaincy flips `isCaptain()` for both the old and new captain correctly. Also had to update three pre-existing tests (`test2.js`, `test5.js`, `test6.js`) whose assertions pointed at the old board-tab/Dagens-tab locations for role-assignment and route-editor UI — not regressions, just relocated UI the old tests hadn't been told about yet; full suite re-run clean afterward.
- **🎣 Fiske & Handel / "Skärgårdshandlaren" — new fishing-and-trading minigame (added 2026-07-15):** user asked for a brand-new archipelago-themed game — fish and gather, sell the catch, use the money to buy fuel and boat upgrades (better sails, a fridge, etc.). Brainstormed as a pitch first, then scoped via two explicit choices: **shared per-boat economy** (not per-device) and **a small capped real-trip-points bonus** on top of the new kronor currency (both user-picked "Recommended" options). New nav entry `["trader","🎣","Fiske & Handel"]` in `NAV_MORE`, its own tab with 4 internal sub-screens switched via a `.catChips` segmented control (`traderTab` state, reusing the same chip pattern as the 🔭 Spana category switcher).
  - **Data model:** new `boats.workshop jsonb not null default '{}'::jsonb` column (migration applied live, `schema.sql` updated) holding `{kronor, fuel, rodLevel, fridge, sails, net, netAt, catch:[{k,qty,day}]}` for the whole boat — shared by the whole crew the same way `boats.roles`/`boats.route` already are (read-modify-write on `myBoat()`, no new realtime channel, "eventually consistent on next load" — consistent with the existing trust model and how role/route edits already behave for other crew members). `myWorkshop()` reads with defaults merged in; `saveWorkshop()` persists (localStorage in LOCAL_MODE, `SB.from("boats").update()` otherwise).
  - **🎣 Fiske (fishing):** costs 1 bensin (fuel) to start a ~60s session (+25s with Bättre segel). Within a session: "Kasta ut" → a random 0.7–1.8s bite delay → a strike-bar minigame appears (an orange marker sweeping a bar via a pure triangle-wave function of elapsed time, `castPosition()`) → "Dra upp!" reads the marker's position *at click time* and resolves hit/miss/perfect (`resolveCast()`). A wider rod (`strikeZoneWidth()`, gated by `rodLevel` 0–3, `ROD_NAMES`/`ROD_COSTS`) makes the zone easier to hit and unlocks rarer species (`FISH_SPECIES`, weighted random via `rollSpecies()`, `minRod` gates strömming/abborre → gädda → torsk/lax). Catching the rare lax grants a real capped trip-points bonus (`FISH_PTS=3`, `kind='bonus'`, `ref='fish:<date>'`, once/sailor/day — same `one_per_sailor` unique-index pattern as dishes/role tasks, no new index needed) plus confetti.
  - **🦪 Samla (gather):** free, no fuel, no timing skill — a single "Leta vid stranden" button with a ~1s "Letar…" delay and an 80% chance of finding blåmusslor/bär/kräftor (`GATHER_ITEMS`). Deliberately the "always something to do to bootstrap money" on-ramp, distinct pacing from Fiske so there's a free option even at zero fuel/zero kronor.
  - **🏪 Marknaden (market):** sells the shared catch. Prices rotate **daily and deterministically** per species (`dailyPriceMult()`, same date-hash pattern as the existing daily quiz/surprise rotation) so "today's prices" are consistent for the whole crew without needing a server round-trip. Spoilage (`applySpoilage()`): any catch not sold by the next calendar day is discarded automatically on next Marknaden visit — *unless* Kylskåp (fridge) is owned, which is the whole point of that upgrade.
  - **⚓ Verkstaden (workshop):** spend kronor on ⛽ Bensin (`FUEL_PACK`=5 units per purchase), 🎣 rod tiers, ❄️ Kylskåp (stops spoilage), ⛵ Bättre segel (+25s fishing sessions), 🕸️ Nät (passive income — `netAccrued()` computes kr accrued since `netAt`, capped at `NET_CAP_HOURS`×`NET_RATE`=24 kr so it can't be left to farm indefinitely offline; a "Töm nätet" button collects it).
  - Built for testability: the core timing/economy logic is pure top-level functions (`castPosition`, `resolveCast`, `strikeZoneWidth`, `rollSpecies`, `dailyPriceMult`, `sellPrice`, `addCatch`, `applySpoilage`, `netAccrued`) independent of the `setInterval`/`setTimeout`-driven UI animation, so the actual hit/miss/pricing/spoilage math is unit-tested with exact synthetic inputs rather than fighting real timers in jsdom. Verified via a new `test_trader.js`: all pure functions directly (including a statistical check that `rollSpecies` never returns a rod-gated species below its `minRod`, and does return the rare species often enough across 300 rolls at higher rod levels), the full purchase/sell/net-collect economy flow, that two different crew members logging into the *same* boat see the *same* shared kronor/fuel/upgrades (the core "shared economy" requirement), and a UI smoke test driving one real fishing session end-to-end (start → cast → wait for a real bite → reel in) plus the Samla and Verkstad screens via actual DOM clicks. Also visually verified with Playwright + real Chromium at a 390px mobile viewport (`trader_visual_check.js`) — no horizontal overflow on any of the 4 screens, strike bar and cards render as intended.
- **Fiske & Handel reworked into a walkable village scene (same day, 2026-07-15, follow-up):** two fixes requested after first shipping the tab-based version above. (1) **Moved into 🕹️ Spelrum as a 6th arcade tile** (`["trader","🎣","Fiske & Handel","Fiska, samla & handla"]` in `renderArcade()`'s game list) instead of a separate `Mer` entry — it's a game, so it belongs with Tetris/Memory/Match-3/Ålen/Uno-poäng. `startTrader(box)` now mounts into `#arcadeBox` like the other games; the `NAV_MORE`/`VIEW_IDS`/`render()` entries for the old standalone `trader` tab were removed. (2) **Replaced the `.catChips` tab UI with an actual walkable top-down scene** — the user's vision: walk across the beach to fish small fish or gather, walk to the bryggan to take the boat out for bigger fish, walk into the village for the market and an outdoor workshop. Scoped via two more explicit choices: **tap-to-walk** movement (not a D-pad) and **two fish-pool tiers** (beach = free, common fish only; boat = costs fuel, unlocks the full rarer/bigger pool) — both user-picked over the alternative option offered.
  - **The scene:** a single CSS-gradient background (sky/sand/dock/grass, `.traderScene`) with 4 tappable landmarks (`TRADER_LANDMARKS`: 🏖️ Stranden, ⚓ Bryggan, 🏪 Marknaden, ⚒️ Verkstaden, each with fixed `{x,y}` percent coordinates) and a player avatar rendered as **the sailor's own emoji** (`me.emoji`), absolutely positioned and CSS-transitioned (`transition:left .5s,top .5s`) so movement glides rather than snaps. Tapping a landmark icon or any open point on the scene calls `traderWalkTo(x,y,onArrive)`, which updates the avatar's `style.left/top` directly (not via an innerHTML rebuild, so the CSS transition can actually play) and schedules `onArrive` via `setTimeout` at a duration scaled to walking distance (250–900ms) — landmark taps pass a callback that opens that landmark's panel on arrival; open-water taps just walk with no callback (free exploration).
  - **Persistent scene, re-rendered panel:** `startTrader(box)` builds the scene markup (landmarks + avatar) **once** per arcade-game activation — critical because the rest of the app's convention is "rebuild everything via `innerHTML` on every render," which would destroy the avatar element and kill the walking animation on every state change. Only a dedicated `#traderPanel` child div is rebuilt when a landmark's content changes (`renderTraderPanel()`, dispatching on `traderPanelKind`); the scene itself is never touched again until the arcade tile is reselected (matches how every other arcade game already resets fresh on reselection, so this isn't a new inconsistency).
  - **Beach → chooser → Fiske (free, small fish) or Samla:** walking to the beach opens a small chooser card (`renderBeachPanel()`, `traderBeachMode` sub-state) offering "🎣 Fiska (gratis)" or "🦪 Samla på stranden". Beach fiske reuses the exact same strike-bar minigame as before but rolls against a new, smaller `BEACH_FISH_SPECIES` pool (strömming/abborre only) and never touches `fuel`.
  - **Bryggan → boat trip (costs fuel, full/rare pool):** walking to the dock shows a "⚓ Bryggan" card; "Åk ut och fiska (−1 ⛽)" deducts fuel and starts the same fishing session but rolling against the full `FISH_SPECIES` pool (gädda/torsk/lax included, still `rodLevel`-gated) — this is the mechanical payoff for "take the boat out for bigger fish." `startFishSession()` now takes a `mode` ("beach"|"boat") threaded through to `reelIn()`'s species roll.
  - **Marknaden/Verkstaden:** same panel content and purchase logic as before, just triggered by walking to their landmark instead of tapping a chip, each with a "⬅ Gå vidare" button that closes the panel (`traderShowPanel(null)`) so the player can keep exploring.
  - Verified via an updated `test_trader.js` (landmark taps replace chip clicks; new checks that the avatar shows the sailor's own emoji, that beach fiske costs no fuel while boat fiske via the bryggan does, and that tapping open water with no real jsdom layout doesn't crash) — full pure-function/economy/shared-boat coverage from the original build carried over unchanged since none of that logic moved. Also re-verified visually with Playwright + real Chromium (`trader_scene_visual.js`): scene, avatar, and all four panels render correctly with zero horizontal overflow at 390px, including a real pixel-coordinate tap-to-walk (not just landmark-button taps) exercising the `getBoundingClientRect()` math that jsdom can't validate.
- Ranks: Jungman 0 → Lättmatros 30 → Matros 60 → Båtsman 130 → Styrman 220 → Skeppare 320 → Kommodor 420.
- **Captain:** each boat has its own `boats.captain` (name match, case/space-insensitive) → sees "Ändra ruttordning" on board; reorder syncs live to that boat's crew only (goal harbor locked as last stop). Challenges keyed by harbor NAME (`cp:<name>`) so they follow reordering. Sun-Day's captain is Martin; new boats auto-captain their creator.
- UI: game HUD (avatar+rank chip → profile; gold ⭐ points coin; blue ⛵ crew coin), XP bar to next harbor, wind bar (Open-Meteo, cached offline, tap to refresh), bottom nav (5 + Mer sheet), splash screen, wave-sweep view transitions, confetti/floating-points/pulse juice.
- Views in Mer: Skeppschatt (realtime; @gemma → gemma_bridge.py), Galleri (logbook photos), Törnrapport (per-day sailing stats from track_points + per-member activity + crew totals), Navigationsskolan, Byssan (8 recipes), Frågesport, Läten (audio players w/ external fallback), Sjötermer (diagrams + boat specs), Live AIS (Fintraffic, often CORS-blocked → fallback text), Seglandet 2026 (harbor guide + Leaflet map).
- Map (Seglandet): Leaflet + OSM, markers for 14 PLACES + Storören; overlays: Väylävirasto WMS fairways (default ON — **layer names `vaylat,vaylaalueet` unverified**, check they render!) + OpenSeaMap seamarks. Buttons: ⛶ fullscreen, 📍 follow (live position+speed), ⏺ track recording (batches to `track_points` every 45 s), 🛤️ today's track replay. Offline fallback = schematic SVG.
- Harbor guide: 14 route harbors (fee+services+shared star-rating logbook with photo upload, client-side resize to 1280 px JPEG) + 9 alternatives + 17 more behind "+ Fler gästhamnar". Prices: Kasnäs from 34 €, Helsingholm 20 €, Högsåra 25+5 €, Ekenäs 25 €, Näsby 20 € all-incl <34 ft, Norrby 27 € (sourced); rest marked "ca".

## ⚠️ Open items / known issues

1. **UNVERIFIED FULL-FILE SYNTAX.** The Cowork sandbox's file mirror of `index.html` was frozen during the last work session, so the final ~10 edits (tracker, report, GPS unlock, facit buttons, map layers, Barösund) were verified only by isolated unit tests (all green) and inspection — NOT by a full `node --check` on the assembled file. **First action in a new session: extract the `<script>` block and run `node --check`, then fix any error.** Roope must also smoke-test locally (double-click index.html) before deploying.
2. **Live site is stale.** sunday-spaning.netlify.app still runs v1.0 (first deploy). Everything since (v2 skin, chat, captain, tracker…) ships with the next folder drag.
3. **Bird sounds not bundled.** Cards look for `sounds/<scientific-name>.mp3` (e.g. `haliaeetus-albicilla.mp3`), fall back to Xeno-canto links. Xeno-canto blocks bots (Anubis) — files must be downloaded manually by Roope.
4. **Väylävirasto WMS layer names unverified** (see Map above).
5. **Jarvis vault sync pending.** Two `vault_remember` calls timed out (Gemma server offline 11.7). Full history is in Claude's auto-memory (`robert-sailing-game` memory) and mirrored here.
6. Pop-quiz answered wrong still writes a 0-point log (marks the day used) — intended, but crew may ask.
7. `logs.kind='bonus'` is allowed by schema but unused/not duplicate-protected.

## Conventions for future edits

- Single-file app; all JS in ONE `<script>` at the end of `index.html`. Function declarations (hoisted) — order rarely matters; top-level `const/let` data lives at the top of the script.
- All UI text in Swedish, playful nautical tone. Points refs: `spot:<cat>:<idx>`, `cp:<harborName>`, `knot:<i>`/`nav:<i>`, `quiz:<date>`/`pop:<date>`, `sur:<date>`, `<game>:<date>`.
- Supabase MCP is connected in Roope's Cowork — use `apply_migration` for DDL, `execute_sql` with `set local role anon` to test RLS (supabase.co is NOT reachable from the sandbox shell).
- Sandbox mirror of index.html can lag minutes behind edits: verify new logic in a fresh scratch `.js` file (they sync instantly), run with node, delete after.
- Roope's preference: save agreed decisions/learnings to the Jarvis vault (`vault_remember`), problems to Knowledge Base.

## Wishlist / not built

- Bundle bird mp3s (manual download) · PWA manifest + service worker for true offline install · export Törnrapport as PDF/share text · date picker for track replay · sound effects · deleting/moderating chat & logbook posts (currently insert-only from app).
